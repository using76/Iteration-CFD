# Provenance — where every file came from

meteor-cfd is source-available under the Meteor Simulation Source-Available
License 1.0 (see ../LICENSE). This file is the per-file record that
[`../LICENSING.md`](../LICENSING.md) §6 phase 4 calls for: for each source
file, what it was written from.

Three categories, and nothing falls outside them:

- **literature** — implemented from the papers named in the file's own header,
  via [`SPEC-LIT.md`](SPEC-LIT.md), which carries the citations.
- **original** — designed here. No external source, permissive or otherwise.
- **format** — reads or writes the OpenFOAM ASCII case format. A file format is
  not a work of authorship, and interoperability is the whole purpose.
- **carried over** — moved across from this project's own earlier C++ when the
  crate became Rust, rather than rewritten. See the note below; this matters,
  and it is why the category exists.

**No file in this tree was written from GPL-licensed source.** The numerical
core that was is gone; see `LICENSING.md` for what was removed and why.

## What "carried over" means, and why it is not a hole in the audit

The crate was Rust-with-CUDA before the relicensing rewrite, and it had been
ported from an earlier C++ version of the same project. When the GPL-derived
numerics were deleted and rewritten, three groups of files were **not**
rewritten, because nothing in them was derived from anyone else's code:

| Carried over | What it does |
|---|---|
| `src/io/*` | reads and writes the ASCII case format |
| `src/blockgen.rs` | generates structured meshes and writes them in that format |
| `src/bin/*` | argument parsing, case loading, reporting loops |

Their headers used to say *"Transcribed from `gpu/common/io/foam_io.cu`"*, and
one said the deleted C++ was *"the specification"* — wording that is accurate
about the mechanics, useless to a reader now that the path is gone, and
alarming in exactly the way the numerics turned out to deserve. They now state
the real chain: the C++ they came from was itself written from the case format
as it appears in data files, not from any CFD code's source.

The distinction that matters: the deleted C++ had its **numerics** transcribed
from OpenFOAM. Its **I/O layer and mesh generator** were not — they were
written from data files, which is why they are here and the numerics are not.
This paragraph exists because an audit of this repository flagged the
contradiction between the old headers and this document. The headers were
wrong; the classification was right.

---

## Numerical core — literature

Each of these carries its own citation header. The papers are listed in
`SPEC-LIT.md`; the section column says which part of it specifies the file.

| File | SPEC-LIT | Primary sources |
|---|---|---|
| `src/mesh/geometry.rs` | §2 | Jasak (1996) §3.2, §3.3.1, §3.4.2; Moukalled et al. (2016) §6.4, §8.6.4; Ferziger & Perić §8.6 |
| `cuda/fv.cu`, `src/fv.rs` | §3, §7 | Jasak (1996) ch. 3; Moukalled et al. ch. 8, 11, 12, §15.4; Patankar (1980) ch. 4–6; Ferziger & Perić §6.3.2; Sweby (1984); van Leer (1977, 1979); van Albada et al. (1982); Roe (1986); Darwish & Moukalled (2003) |
| `cuda/ldu.cu`, `src/ldu_ops.rs` | §1, §3, §4, §5.2 | Jasak (1996) ch. 3; Patankar (1980) §4.2–4.9; Moukalled et al. ch. 8; Saad (2003) §3.4 |
| `cuda/field.cu`, `src/field_ops.rs` | §4 | the Robin boundary form; the representation itself is *DESIGN* |
| `cuda/solver.cu`, `src/solver.rs` | §8 | Saad (2003) §6.7, §7.4.2, ch. 10, §12.4; van der Vorst (1992); Hestenes & Stiefel (1952) |
| `cuda/turbulence.cu`, `src/turbulence.rs`, `src/models/*` | §6.1–6.3, §17 | Launder & Spalding (1974); Wilcox, *Turbulence Modeling for CFD*; Menter (1994); Rodi (1987) and Henkes, van der Vlugt & Hoogendoorn (1991) for the buoyancy production `G_b` and its `C_3` |
| `cuda/sst.cu`, `src/models/k_omega_sst.rs`, `src/models/k_omega_sst/kernels.rs` | §6.3, §6.6 | Menter (1994); Menter, Kuntz & Langtry (2003) — the **2003 revision** is what is implemented, and the file says which two terms differ; Bradshaw, Ferriss & Atwell (1967) for `tau = a_1 k`; Patankar (1980) §4.2 for the cross-diffusion linearisation |
| `src/models/launder_sharma.rs`, the §33 kernels in `cuda/turbulence.cu` (`turbNutLaunderSharma`, `turbLsSqrtPositive`, `turbLsDTerm`, `turbLsGradGradUMagSqr`, `turbLsETerm`, `turbLsEpsilonSources`) | §33 | Launder & Sharma, *Letters in Heat and Mass Transfer* 1 (1974) 131–138; Patel, Rodi & Scheuerer, *AIAA J.* 23 (1985) 1308 (background on the low-Re family). Coefficients are §6.1's own, reused unchanged; the `grad(grad U)` route (Gauss gradient of the already-computed velocity gradient, boundary faces zero-gradient extrapolated) is *DESIGN*, as SPEC-LIT §33.1 itself marks it |
| `src/walldistance.rs`, `turbWallDistance` in `cuda/turbulence.cu` | §6.6 | Tucker, *Applied Mathematical Modelling* 22 (1998) 293–305 — one Poisson solve, no search |
| `cuda/les.cu`, `src/les.rs`, `src/models/les.rs` | §6.5, §16 | Smagorinsky (1963); Nicoud & Ducros (1999); Deardorff (1970, 1980); Scotti, Meneveau & Lilly (1993); van Driest (1956). **The Deardorff kernel follows the algebraic form used by FDS (NIST, public domain — see `reference/fds`, `Source/velo.f90`, and the FDS Technical Reference Guide), used with thanks and acknowledged in the file header.** Its unstructured-mesh test filter is ours. |
| `cuda/wallfunctions.cu`, `src/wallfunctions.rs` | §6.4, §15.1–§15.3, §15.5, §29.2, §29.3, §30.1 | Launder & Spalding (1974); Spalding, *J. Appl. Mech.* 28 (1961) 455; Kader, *Int. J. Heat Mass Transfer* 24 (1981) 1541 (the exponential blend); Menter & Esch (2001) and Popovac & Hanjalić, *Flow Turbul. Combust.* 78 (2007) 177 (blending precedent); Cebeci & Bradshaw, *Momentum Transfer in Boundary Layers*, Hemisphere (1977) — the rough-wall downshift `dB(Ks+, Cs)`; Jayatilleke, *Prog. Heat Mass Transfer* 1 (1969) 193–330 — the thermal sublayer-resistance correction `P(Pr/Pr_t)`; **Werner & Wengle, "Large-eddy simulation of turbulent flow over and around a cube in a plate channel", 8th Symp. Turb. Shear Flows (1991)** — the LES wall model `tau_w_werner_wengle`/`nut_wall_werner_wengle`, integrated-and-inverted over the first cell rather than solved by Newton iteration |
| `src/energy.rs` (thermal wall wiring only — the low-Mach formulation itself is cited separately below) | §29.3 | Jayatilleke (1969), as above; the law and its device kernel live in `src/wallfunctions.rs`/`cuda/wallfunctions.cu`, this file owns only which faces get it and when the Robin triple is rewritten |
| `src/wallfunctions.rs` (`dittus_boelter_nu`, `gnielinski_nu`, `gnielinski_f`), `src/bin/validate.rs` (`check_nu_correlations`, `check_thermal_wall_function_gate_verdict_replay`, `check_resolved_leg_gate_verdict_replay`) | §32.3, §32.4, §35 | Dittus & Boelter, *Univ. Calif. Publ. Eng.* 2 (1930) 443 (reprinted in *Int. Commun. Heat Mass Transfer* 12 (1985) 3); Gnielinski, *Int. Chem. Eng.* 16 (1976) 359 — the two independent, published turbulent-pipe-flow Nusselt-number correlations SPEC-LIT §32's redesigned thermal-wall-function gate compares a live measurement against, in place of comparing two runs of this code against each other. The resolved leg's own replay (SPEC-LIT §35, once the bulk-temperature thermostat gave it an actual steady state to measure) is asserted against Dittus-Boelter only — it is honestly outside Gnielinski's tighter band, reported as a `c.note` rather than hidden or forced |
| `src/models/registry.rs`, `src/models/coupled.rs` | §6, §6.3, §6.5, §6.6, §13.4, §16, §17, §30.2 | Launder & Spalding (1974); Wilcox; Menter (1994, 2003); Smagorinsky (1963); Nicoud & Ducros (1999); Deardorff (1980); Rodi (1987) and Henkes, van der Vlugt & Hoogendoorn (1991) for `G_b`. `registry.rs` *dispatches* to `kOmegaSST` and to the three LES closures rather than naming them in a refusal, and refuses what is still missing; `coupled.rs`'s `CoupledTurbulence` is what lets `src/bin/buoyant.rs` (a coupled solver, not a standalone RAS/LES driver) reach that same dispatch instead of constructing `KEpsilon` directly regardless of what the case asked for — `src/bin/fire.rs` does not use this trait yet and refuses `kOmegaSST`/LES by name (§13.4), its combustion mixing-time closure needing `epsilon` directly being the stated reason; see the *DESIGN* table for the trait itself |
| `src/field.rs`, `src/field_setup.rs` (boundary conditions) | §4, §13.4, §15.2, §15.5, §15.6 | Launder & Spalding (1974) for `k = 3/2 (I\|U\|)²` and `ε = C_mu^{3/4} k^{3/2}/L`; Wilcox for the ω form |
| `cuda/momentum.cu`, `src/momentum.rs` | §5.1, §9 | Rhie & Chow (1983); Moukalled et al. §15.6; Ferziger & Perić §7.5 |
| `cuda/simple.cu`, `src/simple.rs` | §5.2–5.4, §14 | Patankar & Spalding (1972); Patankar (1980) ch. 6; Van Doormaal & Raithby (1984); Issa (1986); Ferziger & Perić §7.4 |
| `src/scalar_transport.rs` | §3, §18 | the same assembly with an effective diffusivity, plus the volumetric sources of §18 |
| `cuda/sources.cu`, `src/sources.rs` | §3.4, §18 | Patankar (1980) §4.2; Ward (1964) for Darcy–Forchheimer |
| `cuda/species.cu`, `src/species.rs` | §19 | the `N-1` formulation, boundedness and sum-to-one |
| `src/reference.rs` | §3, §10 | the CPU mirror, written as scatter loops so its structure differs from the device gather |
| `src/potential_flow.rs` | — | Ferziger & Perić §7.1 |
| `cuda/rheology.cu`, `src/rheology.rs` | §38 | Ostwald (1925) and de Waele (1923); Cross, *J. Colloid Sci.* 20 (1965) 417; Carreau, *Trans. Soc. Rheol.* 16 (1972) 99; Yasuda, Armstrong & Cohen, *Rheol. Acta* 20 (1981) 163; Herschel & Bulkley (1926); Casson (1959); Papanastasiou, *J. Rheol.* 31 (1987) 385; Bercovier & Engelman (1980); Frigaard & Nouar, *J. Non-Newtonian Fluid Mech.* 127 (2005) 1; Bird, Armstrong & Hassager (1987); Chhabra & Richardson (2008) for Buckingham–Reiner |
| `src/contact_angle.rs` (with the §39 kernels in `cuda/vof.cu`) | §39 | Young (1805); Huh & Scriven, *J. Colloid Interface Sci.* 35 (1971) 85; Voinov (1976) and Cox, *J. Fluid Mech.* 168 (1986) 169; Hoffman (1975); Jiang, Oh & Slattery, *J. Colloid Interface Sci.* 69 (1979) 74; Afkhami, Zaleski & Bussmann, *J. Comput. Phys.* 228 (2009) 5370 (named, not implemented); Sui, Ding & Spelt (2014); Washburn, *Phys. Rev.* 17 (1921) 273 |
| `cuda/ke_variants.cu`, `src/models/ke_variants.rs` | §40, §41 | Shih, Liou, Shabbir, Yang & Zhu, *Comput. Fluids* 24 (1995) 227, read as **NASA TM-106721 / ICOMP-94-21 (1994)** (US government work, public domain, <https://ntrs.nasa.gov/citations/19950005029>) — the journal version is paywalled and was NOT read; Yakhot, Orszag, Thangam, Gatski & Speziale, *Phys. Fluids A* 4 (1992) 1510, read as **ICASE 91-65 / NASA CR-187611 (1991)** (<https://ntrs.nasa.gov/citations/19910021152>); Yakhot & Orszag, *J. Sci. Comput.* 1 (1986) 3; Reynolds, AGARD Report 755 (1987) and Lumley, *Adv. Appl. Mech.* 18 (1978) 123 for realizability; Patankar (1980) §4.2. `A_0 = 4.04` is DERIVED in SPEC-LIT §40.3, not transcribed |
| `cuda/twostep.cu`, `src/twostep.rs` | §42, §43 | McGrattan, McDermott & Floyd, *Proc. ISFEH10* (Oslo, 2022), "A simple two-step reaction scheme for soot and CO" — **NIST, US public domain**, PDF fetched from <https://tsapps.nist.gov/publication/get_pdf.cfm?pub_id=927294> and read in full; FDS Technical Reference Guide (NIST SP 1018) Combustion chapter and FDS Validation Guide Experiment chapter, read locally from `reference/fds/Manuals/` (public domain per `reference/fds/LICENSE.md`) — **manuals only, no FDS source code was opened**; Beyler, *SFPE Handbook*, 5th ed. (2016), ch. "Flammability Limits", for the critical flame temperatures, as quoted by both NIST sources; Morehart, Zukoski & Kubota, NIST-GCR-90-585 (1991), as quoted by the FDS TRG; Huggett's `dH/r_O2` as stated by the FDS TRG; Magnussen & Hjertager (1977) and Patankar (1980) §4.2 for the rate and the implicit sink §27 already uses. The propane and methane coefficients are DERIVED in SPEC-LIT §42.4 from the paper's own Eq. (2)-(3), not transcribed |
| `src/cht/flow.rs`, `src/cht/flow/tests.rs`, and SPEC-LIT §59's additions to `src/energy.rs` (`ConjugateEnergy`, `Energy::attach_conjugate` and the four blends of (S59.3)) plus the `fldAdd` kernel in `cuda/field.cu` | §59, §60 | **ORIGINAL** — a driver and four elementwise blends, not new numerics: every equation reached is specified elsewhere in `SPEC-LIT.md` (§5's SIMPLE loop, §9's face body force, §26's energy equation, §46's conduction, §47's interface) and implemented elsewhere in this crate. Written from: **Nek5000 (BSD-3, UChicago Argonne LLC; licence fetched and read) — DOCUMENTATION only**, `nek5000.github.io/NekDoc/theory.html`, for the single-energy-equation-over-`Omega_f ∪ Omega_s` framing §47.4 already adopted; **no Nek5000 source was read**. Patankar (1980) §4.2.3 and §6.7. Published numbers, both cited at the point they are used and both labelled: **de Vahl Davis**, *Int. J. Numer. Meth. Fluids* **3** (1983) 249–264, DOI `10.1002/fld.1650030305` — the four benchmark Nusselt numbers of Gate 59-A, quoted from **Qi et al., *Nanoscale Research Letters* 8 (2013) 56**, DOI `10.1186/1556-276X-8-56`, Table 3 (open access), because the primary is paywalled; and **Belazizia, Benissaad & Abboudi**, *Adv. Theor. Appl. Mech.* **5** (2012) 179–190 (open access, `m-hikari.com`) — the SECONDARY table §60.5's Gate 5 compares against, because **Kaminski & Prakash (1986), DOI `10.1016/0017-9310(86)90017-7`, is paywalled and no open-access copy was found** (Unpaywall reports `is_oa: false` with no OA location); §60.5 records exactly what was searched and that the primary table was never read. **OpenFOAM, SU2, preCICE, Code_Saturne, deal.II and MOOSE are GPL/LGPL and were not opened.** The fluid region's case format is meteor-cfd's own JSONC layout; no other code's case format was consulted |
| the SPEC-LIT §79 additions to `src/cht/flow.rs` (`Openings`, `OpeningReport`, the flux-establishment call and the value-fraction update), `src/io/case_cht.rs` (`kind`, `U`, `inletOutlet`), `src/bin/cht.rs` and `cases/quMudawar.cht.jsonc` | §79 | **ORIGINAL** - four pieces of plumbing, no new numerics: the potential-flow establishment solve is §31's (`crate::potential_flow`), the value fraction is §4's triple switched by the sign of the face flux (`fldInletOutletFraction`, which existed for the `buoyant` and `fire` solvers), and the outflow treatment is `momFluxIsPrescribed` returning false at an `fr = 0` velocity patch. **W. Qu & I. Mudawar, *Int. J. Heat Mass Transfer* 45 (2002) 3973-3985**, DOI `10.1016/S0017-9310(02)00101-1` - the configuration, Table 1's dimensions, Table 2's operating point and assumptions (1)-(6); read in full from the authors' own copy at `engineering.purdue.edu/mudawar/files/articles-all/2002/2002_3.pdf`. **K. Kawano, K. Minakami, H. Iwasaki & M. Ishizuka, ASME HTD-361-3/PID-3 (1998) 173-180** - the measured inlet and outlet thermal resistances Gate 6 is held against. **NOT OBTAINED**: it is an ASME conference volume and no copy was found, so what this gate compares against is **Qu & Mudawar's Fig. 4(b)/4(c), DIGITISED** - the page rendered at 8x, the plot frames and tick marks located by pixel, the marker centres and error-bar caps read off the resulting linear map, reading uncertainty about `0.002` in `R` against error bars of `0.036` and `0.066`. §79.12's Disclosure 1 states this in the specification and `ofgpu-validate` prints it on every run; §60.5's Kaminski & Prakash row and §78's SAE 950283 row are the precedents. **The three water properties the paper does not state** - `rho`, `c_p` and `mu` at 20 C and 1 atm - are standard tabulated values and are §79.12's Disclosure 2, which quantifies what evaluating them at the mean fluid temperature instead would do to the answer (`R_t,out` 0.235 -> ~0.27). **Patankar (1980)** §6.7 for the relaxation pairing, unchanged from §59. **OpenFOAM, SU2, preCICE, Code_Saturne, deal.II and MOOSE are GPL/LGPL and were not opened**; in particular OpenFOAM's `inletOutlet` and `chtMultiRegionSimpleFoam` are the obvious references for every line of this and neither was read. The nine-box decomposition of a micro-channel unit cell (§79.8) and the proof that its same-material couples are the internal-face coefficient they replaced are this document's own |
| `cuda/cht.cu`, `src/cht.rs`, `src/cht/tests.rs`, `src/io/case_cht.rs`, `src/io/case_cht/tests.rs`, `src/bin/cht.rs`, and the `wfThermalConductance` kernel in `cuda/wallfunctions.cu` | §46, §47, §48 | Carslaw & Jaeger, *Conduction of Heat in Solids*, 2nd ed. (1959) ch. I (the anisotropic solid and its affine reduction) and ch. II (two half-spaces in contact — the effusivity-weighted interface temperature); Patankar (1980) §4.2.3 — the **harmonic** face conductivity; Jasak (1996) §3.4.2–3.4.3 — the over-relaxed split, applied here to `K Sf` rather than to `Sf`; Giles, *Int. J. Numer. Meth. Fluids* 25 (1997) 421; Meng, Banks, Henshaw & Schwendeman, *J. Comput. Phys.* 344 (2017) 51 — Theorem 1, the reason Dirichlet–Neumann is NOT implemented; Henshaw & Chand, *J. Comput. Phys.* 228 (2009) 3708; Verstraete & Scholl, *Int. J. Heat Mass Transfer* 101 (2016) 852; Gander, *SIAM J. Numer. Anal.* 44 (2006) 699; Cooper, Mikic & Yovanovich, *Int. J. Heat Mass Transfer* 12 (1969) 279 — the contact-conductance correlation; Yovanovich, *IEEE Trans. Comp. Packag. Technol.* 28 (2005) 182; ASTM D5470-17; Aavatsmark, *Comput. Geosci.* 6 (2002) 405 and Lipnikov et al., *J. Comput. Phys.* 227 (2007) 492 — named in the §46.4 refusal as what would have to be implemented instead. **Nek5000 (BSD-3, UChicago Argonne LLC): DOCUMENTATION only** (`nek5000.github.io/NekDoc/theory.html`), for the single-equation-over-`Omega_f ∪ Omega_s` framing of §47.4 — no Nek5000 source was read. **FDS (NIST, US public domain, `reference/fds/LICENSE.md` read verbatim)**: the discipline that a solid/gas coupling is built from RESISTANCES and exchanges enthalpy, never averaged temperatures; its direction splitting and its `!$OMP CRITICAL` write-back are deliberately NOT taken. **OpenFOAM, SU2, preCICE, Code_Saturne, deal.II and MOOSE are GPL/LGPL and were not opened**; no permissively-licensed unstructured-FV conjugate-heat-transfer implementation with a Robin-triple interface was found, so the derivation is SPEC-LIT §47.2's own. The multi-region case FORMAT (`io/case_cht.rs`, §47.14) and its driver are ORIGINAL - meteor-cfd's own JSONC layout, in the style `docs/05-io-redesign.md` sets out; no other code's case format was consulted |
| `cuda/s2s.cu`, `src/s2s.rs`, `src/s2s/tests.rs` | §49, §50, §51 | G. N. Walton, **NISTIR 6925** (NIST, US Government, **public domain**), <https://nvlpubs.nist.gov/nistpubs/Legacy/IR/nistir6925.pdf> — the double area integral and its dot-product form, the Gaussian-vs-uniform accuracy measurements that force Gauss–Legendre, the relative-separation criterion behind §49.2's order table, the obstruction-elimination test (eq. 11), the row-sum figure of merit, and the `BB104`/Shapiro benchmark numbers; A. B. Shapiro, **FACET UCID-19887** (LLNL, US DOE, **public domain**, DOI `10.2172/5607653`) — the centroid-plus-corner-ray occlusion test and the shadowed benchmark `F₁₂ = 0.115621`; G. P. Mitalas & D. G. Stephenson, **DBR-25**, NRC Canada (1966) — the analytic inner contour integral (1LI) of §49.2b, which is what makes the near-field gate reachable; J. R. Howell, *A Catalog of Radiation Heat Transfer Configuration Factors*, 3rd ed. (free online), entries **C-11** and **C-14**, tracing to Hottel (1931) and Hamilton & Morgan (1952) — both **evaluated as closed forms in the code**, not transcribed as constants; J. Amanatides & A. Woo, *Proc. Eurographics '87* 3 — the uniform-grid DDA; S. Woop, C. Benthin & I. Wald, *JCGT* 2(1) (2013) 65 — the watertight ray/triangle test; J. van Leersum, *Int. J. Heat Fluid Flow* 10(1) (1989) 83 and R. Sinkhorn, *Ann. Math. Statist.* 35(2) (1964) 876 — the symmetric scaling that enforces closure; M. E. Larsen & J. R. Howell (1986), R. I. Loehrke et al. (1995), R. P. Taylor & R. Luck (1995) — the least-squares smoothing family, **named in §49.5 and NOT implemented**; H. C. Hottel & A. F. Sarofim, *Radiative Transfer* (1967) ch. 3 and ch. 5, and M. F. Modest, *Radiative Heat Transfer*, 3rd ed. ch. 5 — the net-radiation exchange method and the closed forms §50.11 gates against; B. Gebhart, *Int. J. Heat Mass Transfer* 3(4) (1961) 341 — absorption factors, **named and not used**; M. F. Cohen & D. P. Greenberg (1985) — the hemicube, **rejected in §49.2 with reasons**; J. K. Salmon et al., *Proc. SC '11* — counter-based RNG, cited only because §49.2 answers the "Monte Carlo could be made reproducible" counter-argument rather than ignoring it; M. R. Vujičić et al., *Proc. IMechE C* 220(5) (2006) 697; T. Karras, *HPG 2012* — the linear BVH, **named as the escalation and not built**; S. F. Potter et al., arXiv:2209.07632 — HODLR compression, **named as the documented next step and not built**; C. Balaji & S. P. Venkateshan (1993, 1994) and M. Akiyama & Q. P. Chong (1997) — the coupled cavity gate, **named and NOT RUN**, with §50.12 saying why. **`github.com/jasondegraw/View3D` was NOT opened**: its README states the originally-public-domain NIST code was relicensed **GPL-3.0**, and it is the top search result for "View3D" — flagged here because its public-domain ancestry makes it look safe. The algorithm is published in full in NISTIR 6925, which is public domain and which *was* read. **OpenFOAM's `radiationModels/viewFactor` was NOT opened**; its `q`-form appears in §50.1 only through this repository's own `docs/01-model-catalog.md`, and §50.1/§50.2 deliberately do the opposite on every axis. **pyViewFactor (MIT)**: public documentation only, as an independent confirmation that a contour kernel plus BVH-accelerated ray occlusion is the right architecture — no code was taken, and it is a Python/PyVista CPU wrapper in any case. `reference/fds` (NIST, public domain) contains **no** view-factor or radiosity code — `grep` confirmed. **Chaparral (Sandia) licence could not be established and was NOT opened**; only its published behaviour in NISTIR 6925's own tables was used |
| `cuda/fan.cu`, `src/fan.rs`, `src/fan/tests.rs`, `src/psychro.rs`, `src/psychro/tests.rs`, `src/dcmetrics.rs`, `src/dcmetrics/tests.rs`, `src/io/case_dc.rs`, `src/io/case_dc/tests.rs`, `src/bin/datacentre.rs` | §52, §53, §54, §55 | **The fan-curve boundary condition of §52.2/§52.3 is DERIVED HERE from first principles** against SPEC-LIT §4's own Robin contract: no permissively-licensed CFD code with a 3-D face-based fan-curve or porous-jump face condition was found, and **OpenFOAM's `fanPressure`, `fan` and `porousBafflePressure` were NOT opened** (GPL-3.0), nor SU2 (LGPL). Where this section says what a commercial code does it comes from that product's USER DOCUMENTATION (ANSYS Fluent User's Guide, "Fan Boundary Conditions" and "Porous Jump Boundary Conditions"; COMSOL CFD Module User's Guide, "Fan and Grille Boundary Conditions"), which is documentation and not source. **FDS 6 (NIST, US Government public domain; `reference/fds/LICENSE.md` read verbatim)**: `Source/hvac.f90` read for TWO points of discipline only — that a fan curve is scaled by `rho/rho_curve` at every evaluation, and that its tabulated branch resolves the operating point by a bisection with `IF (FAN_ITER==20) EXIT`, a data-dependent trip count which §52.7 deliberately does NOT follow. Its `Verification/HVAC/fan_test.fds`, `qfan_test.fds` and their published `.csv` reference values are the external cross-check of §52.12 Gate 52-B — **input decks and results, which are data, not source**. **EnergyPlus (BSD-3-clause style, UIUC/UC Regents/DOE; `LICENSE.txt` fetched and read)**: `src/EnergyPlus/Psychrometrics.hh` gave the NAMING CONVENTION only (`PsyWFnTdbRhPb` → `w_from_t_rh_p`), plus the negative lesson that its caches and its 1651-entry `Tsat(p)` spline exist because those functions are hot on a CPU — on a GPU the polynomial is cheaper, so `crate::psychro` computes and does not cache. **CoolProp (MIT; `LICENSE` fetched and read)**: named in §54.3 as the RP-1485 implementation to check against if the 0.44 % ideal-gas bias ever needs removing; nothing ported. ASHRAE *Handbook—Fundamentals* Ch. 1 (2021) and Hyland & Wexler, *ASHRAE Trans.* 89(2A) (1983) 500 — the `C1`–`C13` saturation-pressure coefficients, transcribed TWICE independently so §54.8 Gate 54-A fails on a dropped digit rather than agreeing with one; Gatley, Herrmann & Kretzschmar, *HVAC&R Research* 14(5) (2008) 655 — `M_a = 28.966 g/mol` and `eps = 0.621945`; Herrmann, Kretzschmar & Gatley, *HVAC&R Research* 15(5) (2009) 961 (RP-1485) — the real-gas enhancement factor, **named and NOT implemented**, with the bias measured and printed. Ward, *J. Hydraulics Div. ASCE* 90(5) (1964) 1 — the Darcy–Forchheimer law §53.1 integrates through a slab; Idelchik, *Handbook of Hydraulic Resistance* 4th ed., Diagrams 8-1 to 8-6 — **NOT opened**; (S53.6)'s thin-plate form is the one published in the open literature, and §53.4 gates its LIMITS rather than either value the design note quotes, one of which it contradicts. Karki, Radmehr & Patankar, *HVAC&R Research* 9(2) (2003) 153; Karki & Patankar, *Building and Environment* 41(6) (2006) 734; Abdelmaksoud et al., *ITherm 2010*; Arghode & Joshi, *IEEE T-CPMT* 3(5) (2013) 800 — the perforated-tile literature behind §53.6's printed caveat; Patankar, *ASME J. Heat Transfer* 132(7) (2010) 073001. Fritsch & Carlson, *SIAM J. Numer. Anal.* 17(2) (1980) 238 — the monotone slope limiter; *Numerical Recipes* 3rd ed. §3.3 — the Hermite basis, reused from `crate::fv`; AMCA 210 / ASHRAE 51 — what a manufacturer's curve is measured at, and hence (S52.13). Buzbee, Dorr, George & Golub, *SIAM J. Numer. Anal.* 8(4) (1971) 722 and Hager, *SIAM Review* 31(2) (1989) 221 — the capacitance-matrix / Woodbury correction that would keep the cuFFT path alive under a fan patch, **named in §52.9 and REFUSED with three reasons**; Vignon-Clementel et al., *CMAME* 195 (2006) 3776 and Esmaily Moghadam et al., *JCP* 244 (2013) 63 — the same patch-integral coupling in the cardiovascular literature, and the source of §52.6's under-relaxation. Herrlin, *ASHRAE Trans.* 111(2) (2005) 725 (RCI) and 114(2) (2008) 182 (RTI); Sharma, Bash & Patel, AIAA 2002-3091 (SHI/RHI); ASHRAE TC 9.9 *Thermal Guidelines* 5th ed. (2021) — the Class A1–A4 envelopes. Wibron, Ljung & Lundström, *Energies* 12(8) (2019) 1473 — **CC-BY-4.0, licence verified live through the Crossref REST API; full text NOT reachable from this environment (MDPI 403)**, so §55.8 gates on the abstract's own RTI relation and says so. ISO/IEC 30134-2's current edition could not be verified (ISO catalogue 403), so **no PUE is computed and no standard number is printed as if it had been checked**. The room case FORMAT (`io/case_dc.rs`) and its driver are ORIGINAL — meteor-cfd's own JSONC layout in the style `docs/05-io-redesign.md` sets out; no other code's case format was consulted |
| `cuda/sa.cu`, `src/models/spalart_allmaras.rs`, `src/models/spalart_allmaras/tests.rs`, `cuda/des.cu`, `src/models/des.rs`, `src/models/des/tests.rs`, `src/bin/sa.rs`, and the `turbVorticityMag`/`turbGradFrobenius` kernels in `cuda/turbulence.cu` | §56, §57, §58 | Spalart & Allmaras, **AIAA Paper 92-0439** (1992) and *La Recherche Aerospatiale* 1 (1994) 5 — the original one-equation model. **Allmaras, Johnson & Spalart, ICCFD7-1902 (2012)**, <https://www.iccfd.org/iccfd7/assets/pdf/papers/ICCFD7-1902_paper.pdf> — a freely distributed conference paper, **read in full**; it is the implementation reference for the `S~` positivity fix (§3.1) and the negative continuation (§3.2). **NASA/TMBWG *Turbulence Modeling Resource*, <https://tmbwg.github.io/turbmodels/spalart.html> and `flatplate.html`/`flatplate_sa.html` — US government-authored DOCUMENTATION, not source, read live**: SA-noft2 and SA-neg to the printed digit, the `r = 10` rule for the `Omega = 0` corner, the far-field `nu_t/nu = 0.210438`/`1.294234` that §56.10 gates against, and the `kappa = 0.41`, `B = 5.0` log-law constants (attributed there to White, *Viscous Fluid Flow*, 1974, p. 472). Rumsey & Spalart, *AIAA J.* 47 (2009) 982. Spalart, Jou, Strelets & Allmaras, *Advances in DNS/LES* (1997) 137 — DES97; Shur, Spalart, Strelets & Travin, *ETMM4* (1999) 669 — `C_DES = 0.65`; Strelets, **AIAA 2001-0879** — SST-DES; Spalart, Deck, Shur, Squires, Strelets & Travin, *Theor. Comput. Fluid Dyn.* 20 (2006) 181 — DDES and grid-induced separation; Nikitin et al., *Phys. Fluids* 12 (2000) 1629; Spalart, *Annu. Rev. Fluid Mech.* 41 (2009) 181. **Herr, Radespiel & Probst, arXiv:2301.07223v2 (2023)** (*Computers & Fluids* 265 (2023) 106014) — **open access, PDF fetched and Appendix A read in full**: it is the source of every IDDES equation here, and of the published statement that `r_dt = 1` characterises the log layer. **Savino, Griffin, Lee, Vijayakumar, Wu & Sprague, arXiv:2603.08875 (2026)** (arXiv non-exclusive distribution licence) — **open access, PDF fetched and §2 read**: `C_DES1 = 0.78`, `C_DES2 = 0.61`, `C_w = 0.15` and the simplified filter width (57.18). **Shur et al., *Int. J. Heat Fluid Flow* 29 (2008) 1638 (IDDES) and Gritskevich et al., *Flow Turbul. Combust.* 88 (2012) 431 (the SST recalibration) are PAYWALLED and were NOT read**; `C_dt1 = 20`, `c_t = 1.87` and `c_l = 5.0` on the SST background come from the design note's reading of the latter and are carried as UNVERIFIED, which the run banner says on its own line. The low-Reynolds correction `Psi` of Shur et al. (2008) is **NOT implemented**, because neither open-access restatement read here carries it — named in §57.5, in `des.cu`'s header and in `ofgpu-validate`'s report rather than silently absent. Travin, Shur, Strelets & Spalart, *Advances in LES of Complex Flows* (2002) 239 — the convection-scheme blending, **named in §57.10's refusal and NOT implemented**. Fröhlich, Mellen, Rodi, Temmerman & Leschziner, *J. Fluid Mech.* 526 (2005) 19 — the periodic hill, **named and NOT RUN**, with §57.12 saying why. Langtry & Menter, *AIAA J.* 47 (2009) 2894 and Menter et al., *Flow Turbul. Combust.* 95 (2015) 583 — named in §58.3's refusal of `kOmegaSSTLM`; neither is implemented. Patankar (1980) §4.2 — the linearisation (56.15) is emitted through. **OpenFOAM's and SU2's Spalart-Allmaras and DES implementations were NOT opened, searched or quoted**; every equation here comes from the papers and the TMR, and (56.14)'s `c_n1` bound, (56.6)'s log-layer identity, (57.9)'s `r_d = 1 + 1/(kappa y+)` and the `tanh` half-ulp saturation argument are all DERIVED in SPEC-LIT rather than transcribed |
| `src/io/output_plan.rs` | §44 | **No external source.** The `output` block is this project's own case format (`docs/05-io-redesign.md` §4.1/§6/§4.6); the writers it drives cite their own formats where they live. ORIGINAL throughout |
| the `_HalfFloat` path in `src/io/vdb.rs` | §45 | AcademySoftwareFoundation **openvdb, Apache-2.0**, at the `v13.0.0` tag: `io/GridDescriptor.cc` (`writeHeader`, `HALF_FLOAT_TYPENAME_SUFFIX`), `io/Compression.h` (`writeCompressedValues`, `HalfWriter`, `RealToHalf`, `truncateRealToHalf`), `tree/{Root,Internal,Leaf}Node.h` (the `toHalf` argument of `writeTopology`/`writeBuffers`), `io/Archive.cc` (`writeGrid`), `Grid.cc` (`setSaveFloatAsHalf`, `META_SAVE_HALF_FLOAT`), `Metadata.h` (`Metadata::write`, `TypedMetadata<bool>`) — the same permissively-licensed source this file's `fp32` path was already written from, cited inline. The binary16 conversion itself is `src/io/nvdb.rs`'s own, not a second implementation |
| `cuda/vof.cu`, `src/vof.rs` | §20, §22 | Hirt & Nichols (1981); Ubbink (1997); Rusche (2002); Zalesak (1979); Brackbill, Kothe & Zemach (1992); Rhie & Chow (1983); Issa (1986); Ferziger & Perić §7.5 |

| `src/timescheme.rs`, `cuda/timescheme.cu` | §3.3, §13 | Crank & Nicolson (1947); Ferziger & Perić §6.3 — the theta method, BDF2 with variable dt, local time stepping |
| `src/precon.rs`, `cuda/precon.cu` | §21 | Saad (2003) §12.4 — multi-colour DIC / DILU |
| `src/walldistance.rs` | §6.6 | Tucker, *Appl. Math. Modelling* 22 (1998) 293 |
| `src/models/k_omega_sst.rs`, `cuda/sst.cu` | §6.3 | Menter, *AIAA J.* 32 (1994) 1598; Menter, Kuntz & Langtry (2003) |
| `src/les.rs`, `src/models/les.rs`, `cuda/les.cu` | §6.5, §16 | Smagorinsky (1963); Nicoud & Ducros (1999); Deardorff (1970, 1980); Scotti, Meneveau & Lilly (1993); van Driest (1956) |
| `src/vof.rs`, `cuda/vof.cu` | §20 | Hirt & Nichols (1981); Zalesak (1979); Brackbill, Kothe & Zemach (1992); Ubbink (1997) and Rusche (2002) theses |
| `src/species.rs`, `cuda/species.cu` | §19 | advection–diffusion with a turbulent Schmidt number |
| `src/sources.rs`, `cuda/sources.cu` | §18 | Patankar (1980) §4.2; Ward, *J. Hydraul. Div. ASCE* 90 (1964) 1 |
| `src/io/schemes.rs` | §11, §12, §13.4 | Warming & Beam (1976); Leonard (1979); Khosla & Rubin (1974); Jasak, Weller & Gosman (1999); Barth & Jespersen (1989); Venkatakrishnan (1993) |
| `src/surface/mod.rs`, `src/surface/stl.rs` | §23.1–§23.2 | The STL format (3D Systems, 1987 — a de facto public specification); Aftosmis, Berger & Melton, *AIAA J.* 36(6) (1998) 952 for the castellation context. Normals are recomputed from the winding; stored normals are never used |
| `src/surface/classify.rs` | §23.3 | Column-parity ray casting with simulation-of-simplicity jitter and a 3-axis majority vote; Barill, Dickson, Schmidt, Levin & Jacobson, *ACM TOG* 37(4) (2018) — the exact solid-angle winding number as the arbiter for cells the vote cannot settle |
| `write_carved_case` and the carver in `src/blockgen.rs` | §23.4–§23.5 | Aftosmis, Berger & Melton (1998), the “castellate” stage only; the FDS precedent (NIST, public domain) for stair-step obstructions |

## Fire physics — literature

`ofgpu-fire`'s low-Mach variable-density solver: SPEC-LIT sections 25-28, plus
§36 (fvDOM), §42/§43 (the two-step scheme and extinction), §61 (soot),
§62 (WSGG spectral radiation) and §63 (the open radiative boundary).

| File | SPEC-LIT | Primary sources |
|---|---|---|
| `src/energy.rs`, `cuda/energy.cu` | §25, §26, §77.6 | Rehm & Baum, *J. Res. Natl. Bur. Stand.* 83 (1978) 297-308 (the `p = p0(t) + p~` split, the divergence constraint and `p0` evolution); Majda & Sethian, *Combust. Sci. Technol.* 42 (1985) 185 (low-Mach acoustic filtering); the FDS Technical Reference Guide (McGrattan et al., NIST SP 1018, public domain — `reference/fds` read and adapted for the SHAPE of the divergence constraint and the sealed/open `p0` bookkeeping, acknowledged per SPEC-LIT §0; no FDS code copied); Patankar (1980) §4.2 for the `Su`/`Sp` linearisation the `EnergySources` registry hands to `fvm_su`/`fvm_sp`. **Kays, *ASME J. Heat Transfer* 116 (1994) 284-295** and **Kays & Crawford, *Convective Heat and Mass Transfer*, 4th ed., ch. 13** now DRIVE code: SPEC-LIT §37's variable turbulent Prandtl number (`PrtModel`, `kays_crawford_prt`, kernel `energyKEffKaysCrawford`), the model behind `k_eff = k + rho cp nu_t/Pr_t(Pe_t)`. *Until §37 they were cited by SPEC-LIT §32.5.5 and `docs/07-fire-solver.md` §1.1 and implemented by nothing - the reference behind an unmeasured hypothesis. The hypothesis has since been measured; see `docs/07-fire-solver.md` §1.1's last subsection for what it found* — **and SPEC-LIT §26.1** adds Moukalled, Mangani & Darwish (2016) §12.4 and §15.4, Patankar (1980) §5.2, and Ferziger & Perić, *Computational Methods for Fluid Dynamics*, 3rd ed. (2002), §5.3, for the conservative/non-conservative pair and the rule that a scalar equation's convecting flux must satisfy the discrete continuity its non-conservative form assumes. That is the section that completed §25.1's `Q` with its conduction term `div(k_eff grad T)` (`Energy::update_conduction_source`), formed with this crate's own `fv::sn_grad_flux`/`fv::fvc_div_surface` off the same face flux `fvm_laplacian` assembles — no new discretisation, no external source |
| `src/combustion.rs`, `cuda/combustion.cu` | §27 | Magnussen & Hjertager, *Proc. Combust. Inst.* 16 (1977) 719-729 (the eddy-dissipation model, `omega_F = C_EDM rho (eps/k) min(Y_F, Y_O2/s)`); Poinsot & Veynante, *Theoretical and Numerical Combustion*, for background; the FDS Technical Reference Guide (public domain) read for acknowledgement that FDS's own mixing-controlled default rests on the same idea — no FDS machinery used; Patankar (1980) §4.2 for the fuel sink's implicit linearisation |
| `src/radiation.rs`, `cuda/radiation.cu` | §28, §62, §63, §64 | Modest, *Radiative Heat Transfer*, 3rd ed., ch. 15 (the P1/differential approximation and its Marshak boundary condition); Patankar (1980) §4.2 for the `T^4 ~= 4 T0^3 T - 3 T0^4` implicit emission linearisation. **§62's per-band P1 and §63's open radiative boundary are ADDITIONS to this file, both opt-in and both leaving the gray zero-gradient path bitwise** — see the `src/wsgg.rs` row for §62's sources; §63's cold-black-surroundings condition is DERIVED here from §4's own Robin triple (the surroundings are a half-space at `T_inf`, not a surface with an emissivity, so it is Dirichlet and not Marshak), and §36.4 already named it as the deferred item this closes. **§64 adds two closed forms, `e2` and `slab_g_mid`** - the second exponential integral and the exact midplane incident radiation of a plane slab between black walls, which are Modest ch. 12-13's standard material and are **DERIVED in SPEC-LIT §64.1 from the RTE rather than transcribed**; `E_2` is evaluated by composite Simpson on `mu` rather than from a tabulated series or an asymptotic expansion, so no constant is copied from anywhere, and the one implementation now serves both §36.7's fvDOM thin-slab gate and §64's banded one. They are REFERENCE arithmetic: no solve reads them |
| `src/fvdom.rs`, `cuda/fvdom.cu` | §36, §62, §63, §65 | Chandrasekhar, *Radiative Transfer* (Dover, 1960), ch. 1 (the RTE); Modest, *Radiative Heat Transfer*, 3rd ed., ch. 16 (the discrete-ordinates method, its finite-volume discretisation, the diffuse-wall condition); Fiveland, *J. Heat Transfer* 106 (1984) 699 (assembling the S_N ordinates' spatial derivatives by finite volume); Truelove, *J. Heat Transfer* 109 (1987) 1048 (the S_N quadrature sets - the S4 directions/weights here are this file's own closed-form solve of the two defining conditions, not a copied table). **`reference/fds/Source/radi.f90` (NIST, public domain) implements the same physics with a different angular quadrature - read for the shape of the wall-reflection bookkeeping, acknowledged in the file header; the quadrature, assembly and wall formula are all derived independently, no FDS code or data used.** **§62's per-band fvDOM and §63's one-sided Dirichlet on the inflow ordinates of an open face are ADDITIONS to this file, both opt-in and both leaving the gray zero-gradient path BITWISE (gated by `fvdom::tests::the_gray_banded_fvdom_path_is_bitwise_identical_to_the_gray_one` and `..._the_default_open_boundary_is_zero_gradient_bitwise_on_fvdom`); §62's sources are in the `src/wsgg.rs` row and §63's condition is derived from §4's own Robin triple.** **§65's `E_2^S4` - the second exponential integral of (64.2) evaluated ON the S4 quadrature - is DERIVED in SPEC-LIT §65.3 from the same single-ray solution §64.1 integrates, and nothing is transcribed: the two identities it rests on, `E_2^S4(0) = 1` and `integral E_2^S4 dtau = 1/2`, are Truelove's own two defining conditions on the set (`sum_m w_m = 4 pi` and `sum_(mu>0) w_m mu_m = pi`), which §36.2 already solves in closed form here. It is REFERENCE arithmetic living in `fvdom::tests` and `src/bin/validate.rs`; no solve reads it.** |
| `src/soot.rs`, `src/soot/tests.rs`, `cuda/soot.cu` | §61 | **Lautenberger, de Ris, Dembsey, Barnett & Baum, *Fire Safety Journal* 40(2) (2005) 141-176**, DOI `10.1016/j.firesaf.2004.10.002` - the laminar-smoke-point model: the mixture-fraction and temperature windows, their four defining conditions, the constants of §61.3 and the smoke-point scaling (61.5). The author manuscript is free at `tsapps.nist.gov/publication/get_pdf.cfm?pub_id=101148` and **was read**; the closed form (61.4) that solves the four stated conditions is DERIVED here, in the same spirit as §36.2's S4 quadrature, and its four conditions are checked back to round-off. Tewarson, *SFPE Handbook* ch. 36 Table A.40 - the post-flame yields, via the FDS Validation Guide (NIST, **public domain**). Magnussen & Hjertager (1977) - §27's own rate, whose heat release the prescribed-yield model reads; their soot-burnout rate is **named in §61.1 and NOT implemented**. Kent & Honnery, *Combust. Flame* 79 (1990) 287; Delichatsios (1993, 1994); Leung, Lindstedt & Jones (1991); Tesner et al. (1971); Khan & Greeves (1974) - cited, not implemented. Brookes & Moss, *Combust. Flame* 116 (1999) 486 and Moss, Stewart & Syed (1989) - the two-equation model, **REFUSED BY NAME with the reason** (a single-step or serial two-step mixing-controlled scheme produces neither C2H2 nor OH); Sivathanu & Faeth, *Combust. Flame* 82 (1990) 211 - what would be needed to manufacture them, cited as the reason not to. Gelbard/Tambour/Seinfeld (1980), Kumar & Ramkrishna (1996), Netzell et al. (2007) - the sectional family, refused by name. Rigopoulos, *Flow Turbul. Combust.* 103 (2019) 565 - the open-access review behind §61.1's table. Talbot et al., *J. Fluid Mech.* 101 (1980) 737 - thermophoretic deposition, **named as the next step and NOT implemented**. **`reference/fds/Source/` implements no soot FORMATION model at all** (FDS prescribes yields), so there was nothing there to take; `soot.f90` covers deposition only and is cited for the boundary condition this section does not implement. **The Chalmers OpenFOAM course report on implementing a two-equation soot model for `sprayFoam` was deliberately NOT opened** although it is the top search hit for Moss-Brookes - it would carry GPL-derived listings. The ANSYS Fluent theory DOCUMENTATION page was read for the Moss-Brookes constants quoted in §61.1's refusal; vendor documentation describing published models, not source |
| `src/wsgg.rs`, `src/wsgg/tests.rs`, `cuda/wsgg.cu` | §62 | **Bordbar, Węcel & Hyppänen, *Combustion and Flame* 161(9) (2014) 2435-2445**, DOI `10.1016/j.combustflame.2014.03.013` - the coefficient set: (62.5)/(62.6), the molar-ratio branch structure and `T_ref = 1200 K`. **The 168 COEFFICIENTS ARE TRANSCRIBED FROM `reference/fds/Source/radi.f90` (NIST, US Government, PUBLIC DOMAIN - `reference/fds/LICENSE.md` read verbatim), `MODULE WSGG_ARRAYS` and the functions `KAPPA_WSGG`, `A_WSGG`, `KAPPA_SOOT`**, and that is stated in both file headers rather than paraphrased: Bordbar's paper is paywalled (ScienceDirect returned HTTP 403 to every attempt), the numbers are DATA rather than expression, and a public-domain transcription is clean on both counts. Everything around them - the band decomposition, the P1 and fvDOM per-band assembly, the transparent-window treatment, the per-band emission floor (62.19), the energy coupling (62.17) and the bitwise gray limit of §62.8 - is derived in SPEC-LIT §62. `KAPPA_SOOT`'s own fit (62.11) is a published correlation, and `radi.f90` records that it is the same function Fluent uses. Hottel & Sarofim, *Radiative Transfer* (1967) - the weighted-sum construction and the window closure `a_0 = 1 - sum_j a_j`. Modest, *Radiative Heat Transfer*, 3rd ed. ch. 11 - the Rayleigh-limit soot coefficient; ch. 15/16 - the per-band RTE. Patankar (1980) §4.2 - the `Su`/`Sp` split. Smith, Shen & Friedman (1982); Coppalle & Vervisch (1983); Taylor & Foster (1975); Felske & Charalampopoulos (1982); Denison & Webb (1995); Johansson et al. (2011); Dorigon et al. (2013); Cassol et al. (2014); Alberti, Weber & Mancini (2018); **Sadeghi, Hostikka, Fraga & Bordbar, *Fire Safety Journal* 125 (2021) 103420**; Rashidzadeh et al., *Fire Safety Journal* 160 (2026) 104606 - the alternatives, every one **RECOGNISED AND REFUSED BY NAME WITH THE REASON** in `SpectralModel::from_name`. Chang & Charalampopoulos, *Proc. R. Soc. A* 430 (1990) 577; Widmann, *Combust. Sci. Technol.* 175 (2003) 2299; Grosshandler, **NIST TN 1402** (public domain) - soot optics and RadCal. `reference/fds/Source/read.f90`'s band and angle defaults are quoted in §62.10 as public-domain corroboration of the cost, and the FDS User Guide's own "experimental feature... very limited verification and validation" verdict on its WSGG implementation is quoted rather than paraphrased. **Bordbar's own emissivity table could NOT be obtained** (ScienceDirect 403), so §62.12's **Gate 1-E** compares (62.1) against **RADCAL** instead - Grosshandler, *RADCAL: A Narrow-Band Model for Radiation Calculations in a Combustion Environment*, **NIST Technical Note 1402** (1993), DOI `10.6028/NIST.TN.1402`, **US public domain**, run from NIST's own `reference/fds/Source/rcal.f90` unmodified by the standalone driver in `../tools/radcal_emissivity/`. The 108 resulting total emissivities are recorded verbatim as `RADCAL_EPS` with the blackbody-window correction they need; **they are DATA produced by a public-domain program, not transcribed expression**, and the driver that regenerates them is in the tree. RADCAL is an INDEPENDENT model rather than truth, and §62.13's verdict says so |

## Lagrangian parcels — literature

The dispersed phase: SPEC-LIT §66. **One-way coupled and inert** — parcels feel
the gas, the gas does not feel them, and nothing here writes a cell field or a
matrix entry, so every Eulerian result in the crate is unmoved by construction
rather than by measurement.

| File | SPEC-LIT | Primary sources |
|---|---|---|
| `src/parcels.rs`, `src/parcels/tests.rs`, `cuda/parcels.cu` | §66 | **Dukowicz, *J. Comput. Phys.* 35 (1980) 229-253**, DOI `10.1016/0021-9991(80)90087-X` — the discrete droplet model: (66.1)'s ensemble of delta functions and the real-valued parcel weight `n_p`. **Crowe, Sommerfeld & Tsuji, *Multiphase Flows with Droplets and Particles*, CRC Press (1998)** — equation of motion (66.2) and the regime argument for which terms survive at `rho/rho_l ~ 1e-3`; **Maxey & Riley, *Phys. Fluids* 26 (1983) 883** — the derivation behind `C_am = 1/2`. **Schiller & Naumann**, *Z. Ver. Deutsch. Ing.* 77 (1933) 318, in the compilation of **Clift, Grace & Weber, *Bubbles, Drops, and Particles*, Academic Press (1978)** — the drag correlation (66.5). **The singularity-free form (66.6) — `K = rho C_d |u_rel|` rather than `C_d` — is DERIVED here**, and so is the `expm1`/`(1-e^-b)/b` form (66.8) of the exponential update and the closed-form terminal balance (66.7) with its `C_am` cancellation; none of the three is transcribed from anywhere. **`reference/fds/` (McGrattan et al., NIST SP 1018-1, US-Government PUBLIC DOMAIN — `reference/fds/LICENSE.md` read verbatim)**, chapter "Lagrangian Particles" and appendix "Fluid-Particle Momentum Transfer" — read for the EXPONENTIAL integration of the linearised drag (66.8), the trapezoidal position update (66.9) and the sub-step CFL bound (66.11). FDS is CPU/MPI Fortran on a Cartesian mesh and answers no GPU question: the SoA pool, the CSR walk, the fixed-geometry grid-stride kernels reading a device-resident count, and the bijective identity are this project's own. **Macpherson, Nordin & Weller, *Commun. Numer. Meth. Engng* 25 (2009) 263**, DOI `10.1002/cnm.1128` — barycentric tracking on a tet decomposition, **NAMED IN §66.6 AS THE FIX AND NOT IMPLEMENTED**; the **paper** was read and **OpenFOAM's implementation of it is GPL-3.0 and was NOT opened**. **Steele, Lea & Flood, OOPSLA 2014, *ACM SIGPLAN Notices* 49(10) 453**, DOI `10.1145/2660193.2660195` — SplitMix64's finalising mix, whose two multiplier constants are the published ones and whose reference implementation (Vigna's `splitmix64.c`) is **public domain, CC0**. It is used in (66.14) as a **bijection**, not as a generator: that is what makes parcel identity unique by construction rather than by a birthday argument, and `the_uid_mix_is_a_bijection` inverts it to prove it. Named and **NOT implemented**, each with a section number: Ranz & Marshall, *Chem. Eng. Prog.* 48 (1952) 141 and 173, and Sazhin, *Prog. Energy Combust. Sci.* 32 (2006) 162 — evaporation, refused by name here and **IMPLEMENTED at §76**, which is why `ParcelPhysics` now has three values; Abramzon & Sirignano, *Int. J. Heat Mass Transfer* 32 (1989) 1605 — its successor; Bai & Gosman, SAE 950283 (1995) and Mundo, Sommerfeld & Tropea, *Int. J. Multiphase Flow* 21 (1995) 151 — the four-outcome wall map, of which `stick`, `spread`, `film` and `splash` were **REFUSED BY NAME WITH THE REASON** here (§66.10) and which is **IMPLEMENTED at §78**, so `stick` is a value now and `weber` selects among all four; `film` and `splash` are still refused and carry two DIFFERENT reasons (§78.11, §78.7); Chan, *J. Fire Prot. Eng.* 6(2) (1994) 79 and Yu, IAFSS 1 (1986) 1165 — the Rosin-Rammler/log-normal size distribution and `D_v50 ~ We^(-1/3)`, unreachable until an RNG exists; Salmon, Moraes, Dror & Shaw, SC '11 article 16 — the counter-based (Philox/Threefry) generator §66.8 names as the prerequisite for all of them, whose reference implementation is BSD-3-Clause and was **not** vendored; Subramaniam, *Prog. Energy Combust. Sci.* 39 (2013) 215 — the three-way convergence statement of §66.1; Elghobashi, *Appl. Sci. Res.* 52 (1994) 309 — the coupling-regime map that says when one-way coupling stops being enough. **NVIDIA cuda-samples (BSD-3-Clause) and AMReX (BSD-3-Clause, LBNL)** were identified as the permissively-licensed prior art for GPU particle containers and sort-by-cell-hash; **neither is used here**, because both deposit through floating-point atomics and this crate forbids them — the fact that the closest permissive prior art is a counterexample on the one property that matters is recorded in §66.14 rather than glossed. **PyFR (BSD-3-Clause) has no Lagrangian particle module at all.** **No GPL-licensed source was consulted**, and in particular **OpenFOAM's `src/lagrangian` tree — the obvious reference for every line of this — was not opened**, nor SU2 (LGPL), nor PySDM/libcloudph++/LIGGGHTS/SPARTA (all GPL) |

## The parcel sort and gather-shaped deposition — literature

Turning the per-cell sum of the PSI-Cell construction from a **scatter** into a
**gather**: SPEC-LIT §67. Still one-way coupled — nothing here writes a
momentum, energy or species source — but the machinery that will carry them is
here, and its whole reason for existing is that the obvious implementation
(`atomicAdd(double*)`) destroys the bitwise reproducibility this crate is
organised around.

| File | SPEC-LIT | Primary sources |
|---|---|---|
| `src/parcels/deposit.rs`, `src/parcels/deposit/tests.rs`, `cuda/parcelsort.cu` | §67 | **Crowe, Sharma & Stock, *J. Fluids Eng.* 99 (1977) 325**, DOI `10.1115/1.3448756` — the particle-source-in-cell construction, equation (67.1): a cell quantity is the sum over the parcels in the cell of `n_p` times the per-droplet quantity, over the cell volume. **Satish, Harris & Garland, IEEE IPDPS 2009**, DOI `10.1109/IPDPS.2009.5161005` — the three-phase radix pass of (67.6)/(67.7): a per-block digit histogram, ONE global exclusive scan over digit-major block counters, and a stable scatter whose destination is a closed form. The **paper** was read; **no implementation of it was opened**, and CUB — whose device-wide radix sort is the obvious call — is unreachable from this build because `build.rs` emits CUBIN with no host translation unit (§67.8(a)). **Merrill & Grimshaw, University of Virginia Technical Report CS2009-14** — the reduce-then-scan decomposition of (67.3); decoupled look-back was **rejected** because it needs an atomically assigned tile order, which is the scheduling dependence §67 exists to remove. **Blelloch, CMU-CS-90-190 (1990)** — the exclusive scan and its work-efficiency argument. **Hillis & Steele, *Commun. ACM* 29(12) (1986) 1170**, DOI `10.1145/7902.7903` — the in-block scan network, chosen *against* work-efficiency because its shape is a function of `blockDim` alone and is therefore deterministic by construction. **Elghobashi, *Appl. Sci. Res.* 52 (1994) 309**, DOI `10.1007/BF00936835` — the coupling-regime map that says what the deposited `alpha_p` of (67.9) is for. **The canonicalisation argument of §67.1 is ORIGINAL**: that sorting on `(cell, uid)` makes the CSR a pure function of the physical state, hence of neither slot order nor injection order nor block count, and that the gate for it must therefore permute the slots while holding the parcel SET fixed. So is the `nCells` sentinel of (67.5), whose lower bound *is* the live count; so is the binary-search CSR of (67.8), which needs no atomic and no scan; and so is the fixed-at-setup ping-pong parity of §67.7, without which a captured graph and the host disagree about which buffer holds the answer. **NVIDIA cuda-samples (BSD-3-Clause) and AMReX (BSD-3-Clause, LBNL)** remain the closest permissive prior art for GPU particle containers and sort-by-cell-hash, and remain **unused for the same reason as in §66**: both deposit through floating-point atomics. **No GPL-licensed source was consulted**, and in particular **OpenFOAM's `src/lagrangian` tree — which contains the obvious reference implementation of a per-cell parcel grouping — was not opened** |

## Two-way coupling of the dispersed phase — literature

Giving the gas back what the parcels took: SPEC-LIT §68. The momentum and
sensible-heat sources, through §18's registries, so that nothing in the solvers
learns that parcels exist. Evaporation, droplet radiation and wall splash are
**refused by name** here rather than approximated.

| File | SPEC-LIT | Primary sources |
|---|---|---|
| `src/parcels/couple.rs`, `src/parcels/couple/tests.rs`, `cuda/parcelcouple.cu` | §68, §77 | **Crowe, Sharma & Stock, *J. Fluids Eng.* 99 (1977) 325**, DOI `10.1115/1.3448756` — the particle-source-in-cell construction (68.1), now evaluated for real. **Patankar, *Numerical Heat Transfer and Fluid Flow*, Hemisphere (1980), §4.2, §7.2** — the `S_u + S_p psi` linearisation and the rule `S_p <= 0`, which (68.10) satisfies **by construction**: `beta_P` is a sum of `n_p m_eff (1 - e^-x)` with `x >= 0`, so no clamp and no sign branch exists in the kernel. **Ranz & Marshall, *Chem. Eng. Prog.* 48 (1952) 141 and 173** — `Nu = 2 + 0.6 Re^(1/2) Pr^(1/3)`; only its **sensible-heat half** is implemented here (68.8); the evaporation the same papers give was refused by name at §68.13 and **IMPLEMENTED at §76**, and §68's refusal of a mass COUPLING survives with a new reason. **Maxey & Riley, *Phys. Fluids* 26 (1983) 883**, DOI `10.1063/1.864230` — the equation of motion whose drag term is the one being returned to the gas. **Elghobashi, *Appl. Sci. Res.* 52 (1994) 309**, DOI `10.1007/BF00936835` — the coupling map that says when two-way coupling is required at all. **`reference/fds/` (McGrattan et al., NIST, US-Government PUBLIC DOMAIN — `reference/fds/LICENSE.md` read verbatim)**: the Technical Reference Guide's appendix *Fluid-Particle Momentum Transfer* for the principle that the gas receives the negative of the droplets' own momentum change, and `Validation/Theobald_Hose_Stream/FDS_Input_Files/Build_Input_Files/{paramfile.csv, build_input_files.py}` — **input decks and a data-generation script, which are data, not solver source** — for the transcription of §68.12's 90 experiments and for the derivations they document (efflux velocity `3.71 sqrt(dP[psi])`, droplet diameter one tenth of the bore, `PRIMARY_BREAKUP_LENGTH` as twice Theobald's Eq. 2). **Theobald, *Fire Safety Journal* 4 (1981) 1-13** — the hose-stream experiments themselves, which §68.12 reports against and **MISSES**, with the miss and its diagnosis on `ofgpu-validate`'s own screen. **The impulse formulation of §68.1 is ORIGINAL to this section**: that the conservative source is the impulse the exponential integrator ALREADY applied, `m_eff (Delta u_p - a_g h)`, rather than the re-linearised `(m_p/tau_p)(u_p - u)` the design note recommends — which is cheaper, exact through sub-steps, cell crossings and wall rebounds, and turns the conservation gate from a tolerance into an identity. So is the observation of §68.3 that the empty-cell deposit is `-0.0` and that `-0.0` is the additive identity that leaves every accumulator bitwise unmoved. So is the argument of §68.4 for a second whole-field registry rather than a `SourceTerm` variant, and the placement of the coherent core of §68.12 as a closed-form vacuum parabola that needs no kernel at all. **SPEC-LIT §77** adds, on the same kernel and the same CSR walk: **Rehm & Baum, *J. Res. NBS* 83 (1978) 297-308** — the low-Mach divergence constraint the vapour's own volume enters; the **FDS Technical Reference Guide**'s chapter *The Divergence* (NIST SP 1018-1, US-Government PUBLIC DOMAIN, `reference/fds`) — the same `D_SOURCE` term in its general variable-molar-mass form, of which §25's constant-`W` gas keeps `mdot/rho` and nothing else, which §77.6 states rather than leaving to be inferred; and the **ASHRAE Handbook — Fundamentals (2017), ch. 1, eq. 33** — the adiabatic-saturation relation gate 77-D is measured against, reached through this crate's own §54 (`psychro::t_wb`) rather than re-fitted. **ORIGINAL to §77**: the finding that there is NO second latent-heat sink — the droplet's budget (76.10) puts every joule the phase change consumed inside the convective heat the gas has already given up, so depositing `q_lat` again would double-count it, and what the gas is actually owed is the arriving mass's sensible enthalpy `cp_g mdot (T_p - T_g)`, 12 % of the latent heat rather than 100 % of it (§77.4); the statement of §77.6 that the ENERGY half of evaporation's effect on the divergence arrives through §25.1's `Q` with no code at all while the VOLUME half does not, which is §26.1's omission with the halves swapped; the additive `update_target_divergence_with`, which leaves `energyTargetDivergence` and its caller untouched so that a run with no dispersed phase is bitwise what it was BY CONSTRUCTION; and the observation that `mdot` is not sign-definite — a droplet in supersaturated air grows — so neither §77 source can be made an implicit sink without the first clamp in this addition (§77.5). **No GPL-licensed source was consulted**, and in particular **OpenFOAM's `src/lagrangian` tree — which contains the obvious reference implementation of a parcel-to-cell source — was not opened**, nor SU2 (LGPL) |
| `src/parcels/evaporation.rs`, `src/parcels/evaporation/tests.rs` (the evaporation half of `cuda/parcels.cu`) | §76 | **Ranz & Marshall, *Chem. Eng. Prog.* 48 (1952) 141-146 and 173-180** — `Nu_0 = 2 + 0.6 Re^(1/2) Pr^(1/3)`, `Sh_0 = 2 + 0.6 Re^(1/2) Sc^(1/3)`, and the `d(D^2)/dt` metric of §76.12's first gate. The papers whose sensible-heat half §68.8 implemented and whose mass-transfer half §68.13 refused by name; this section is the other half, so the refusal list shrank by one. **Spalding, 4th Symp. (Int.) Combust. (1953) 847-864**, and *Convective Mass Transfer: An Introduction*, Edward Arnold (1963) — the mass transfer number `B_M` and the Stefan-flow rate `mdot = -pi d rho_f D_f Sh_0 ln(1 + B_M)`. **Godsave, 4th Symp. (Int.) Combust. (1953) 818-830** — the heat-limited rate of the boiling branch (76.9), which is what is physically true exactly where the diffusion-limited rate diverges. **Abramzon & Sirignano, *Int. J. Heat Mass Transfer* 32 (1989) 1605-1618**, DOI `10.1016/0017-9310(89)90043-4` — `B_T = (1 + B_M)^phi - 1` with `phi = (c_pv/c_pg)(Sh_0/Nu_0)/Le`, the DEFAULT here because `phi ~ 2.07` for water in air and `phi = 1` is what FDS's simplification assumes; their film-thickness corrections `Nu*`/`Sh*` are **NAMED AND REFUSED** (§76.6), because a fixed point is a data-dependent trip count and a captured graph cannot hold one. **Sazhin, *Prog. Energy Combust. Sci.* 32 (2006) 162-214**, DOI `10.1016/j.pecs.2005.11.001` — the modern re-derivation and the survey of which successors earn their flops; its effective-conductivity droplet-interior model is named as what a `Bi ~ 0.1` fuel droplet would need and is **NOT implemented** (§76.8). **Watson, *Ind. Eng. Chem.* 35 (1943) 398-406** — `h_v(T) = h_v(T_b)[(T_c - T)/(T_c - T_b)]^0.38`, whose 1.33 % high reading for water at 25 C is measured and printed rather than absorbed. **Marrero & Mason, *J. Phys. Chem. Ref. Data* 1 (1972) 3-118**, DOI `10.1063/1.3253094` — `D(H2O-air) = 1.87e-10 T^2.072/p[atm]` m²/s over 282-450 K, which sets the default diffusivity, its temperature exponent, and (through the Lewis number) the size of §76.13's wet-bulb gap; the widely quoted `2.42e-5` is 3.6 % away and §76.13 states what choosing it would move. **Hyland & Wexler, *ASHRAE Transactions* 89(2A) (1983) 500-519** — the saturation polynomial, taken from this crate's own §54 (`src/psychro.rs`) rather than re-fitted, so that the two modules cannot disagree about water; its analytic `d(ln p_ws)/dT`, which the device linearisation needs, is **DERIVED here** and checked against a central difference of §54's own function. **Lewis, *Trans. ASME* 44 (1922) 325-340** — the heat/mass transfer coefficient relation whose failure at `Le != 1` is what §76.13's gap is made of. **NIST Chemistry WebBook, SRD 69 (US-Government PUBLIC DOMAIN)** — the water-vapour specific heat and the critical constants. **`reference/fds/` (McGrattan et al., NIST SP 1018-1, US-Government PUBLIC DOMAIN — `reference/fds/LICENSE.md` read verbatim)**, chapter "Lagrangian Particles" and appendix "Development of an Implicit Solution for Droplet Evaporation" — read for the model set; its `B_T = B_M` simplification is offered as `massTransfer spalding` and is **not** the default, and its cell-coupled 3x3 implicit closure is **deliberately not reproduced**, because this section holds the gas frozen and the ordering hazard that closure exists to manage does not arise (§76). **ORIGINAL to this section**: the ordering of (76.10), in which the mass is DERIVED from the energy the exponential integrator actually applied rather than integrated beside it — §68's construction one equation over, and what makes the droplet's own budget `C dT = Q_c - dm h_v` an f64 identity; the factored endpoint difference `rho_l (pi/6)(d_0 - d)(d_0^2 + d_0 d + d^2)` of (76.11), which is exact where `d_0^3 - d^3` loses four orders of magnitude to cancellation; the PINNING of `T_p` at `T_b` in the boiling branch, without which the two branches alternate on a one-ulp comparison and the evaporation rate collapses by ten orders of magnitude — a defect the boiling gate caught; the vaporisation of arrival superheat rather than its deletion; the scan-and-bisect boiling-point solve, forced by the finding that Clausius-Clapeyron with Watson's `h_v` is **non-monotone** above ~545 K for water and returns `p_b` again at the critical point; and the evaporation sub-step bound (76.12). **No GPL-licensed source was consulted**, and in particular **OpenFOAM's `src/lagrangian` tree — which contains the obvious reference implementation of a droplet evaporation model — was not opened**, nor SU2 (LGPL), nor PySDM (GPL-3.0) |
| `src/parcels/impact.rs`, `src/parcels/impact/tests.rs` (the impact half of `cuda/parcels.cu`) | §78 | **C. Bai & A. D. Gosman, SAE Technical Paper 950283 (1995)**, DOI `10.4271/950283` — **PAYWALLED and NOT obtained**; the constants below are the ones its citing literature quotes, which §78.10 and §78.12 both state, and which is the reason every one of them is a CONTROL with a default rather than a constant in the source (§60.5's Kaminski & Prakash row is the precedent for saying so). The four-regime dry-wall impingement map (adhesion / rebound / spread / splash), its Weber-number boundaries `We ~ 2` and `We ~ 20`, and the splash threshold `We_c = A La^(-0.18)` whose `A` is roughness-dependent and is therefore the control `splashA` rather than a constant. It was **named and NOT implemented** at §66.10, where all four of its outcomes were refused; §78 implements the map, so the refusal list shrank by one and the two that remain (`film`, `splash`) carry two DIFFERENT reasons. **C. Mundo, M. Sommerfeld & C. Tropea, *Int. J. Multiphase Flow* 21 (1995) 151-173**, DOI `10.1016/0301-9322(94)00069-V` — the deposition/splashing parameter `K = Oh Re^1.25` and its measured threshold `K = 57.7`, which is the DEFAULT criterion. Their splashing data themselves are **NOT transcribed** and gate 78-D says so: the paper's boundary is a figure, not a table, and §78.10 records that this section has no experimental gate. **A. L. Yarin, *Annu. Rev. Fluid Mech.* 38 (2006) 159-192**, DOI `10.1146/annurev.fluid.38.050304.092144` — the review behind §78.1's statement that neither correlation is better than approximately right, which is why every threshold here is a control. **IAPWS, *Revised Release on Surface Tension of Ordinary Water Substance*, R1-76 (2014)** — `sigma = B tau^mu (1 + b tau)` with `B = 235.8 mN/m`, `b = -0.625`, `mu = 1.256`; a freely published international standard, **implemented and gated against the release's own table** (worst 0.0045 mN/m over 0-300 °C), and the source of the default `sigma` at 293.15 K. **NIST Chemistry WebBook, SRD 69 (US Government, PUBLIC DOMAIN)** — the liquid viscosity of water that sets the default `muLiquid`. Named and **NOT implemented**, each with a section number: **P. J. O'Rourke & A. A. Amsden, SAE 961961 (1996)** and **SAE 2000-01-0271 (2000)**, and **D. W. Stanton & C. J. Rutland, *Int. J. Heat Mass Transfer* 41 (1998) 3037**, DOI `10.1016/S0017-9310(98)00054-4` — the wall-film momentum equation, refused by name in §78.11; the FDS Technical Reference Guide's much cheaper fixed-dripping-velocity film (NIST SP 1018-1, US-Government PUBLIC DOMAIN, `reference/fds`), which needs the RNG §66.8 also does not have; and Bai & Gosman's own STOCHASTIC retained-mass fraction, which §78.7 declines for the same reason and because deleting the un-retained part would break the ledger's exact partition. **ORIGINAL to this section**: the fourth-power decision form `K^4 = We^2 Re` of (78.4b) — an identity, because `Oh Re = sqrt(We)` — which takes Mundo's decision with no `pow` and no `sqrt` and is therefore bit-stable across compute capabilities where the criterion's own form is not (§38.6); the closed-form inverses (78.4c) that make the boundary gate analytic rather than a bisection against the kernel; the observation that a deposit is `remove` plus exactly TWO statements, because §66's pool reclaims no slot and therefore already holds the arriving droplet's diameter and weight; and the resulting ledger (78.6), which is a PARTITION of the pool rather than an accumulator — no atomic, floating-point or otherwise, and "nothing vanishes at a wall" is a statement about bits rather than a tolerance. **No GPL-licensed source was consulted**, and in particular **OpenFOAM's `src/lagrangian/intermediate/submodels/.../PatchInteractionModel` tree — which contains the obvious reference implementation of a Bai-Gosman patch interaction — was not opened**, nor SU2 (LGPL), nor KIVA |

## New I/O formats and machinery — format / literature

| File | What it is |
|---|---|
| `src/io/msh.rs` | Gmsh `.msh` 4.1 reader (tet/hex/prism/pyramid cells, `$PhysicalNames` patches). The FORMAT is published (Gmsh manual) and not copyrightable; `reference/pyfr`'s BSD-3 `pyfr/readers/gmsh.py` was the legal cross-check for blocked `$Nodes`, non-contiguous tags and physical names — no Gmsh source was read |
| `src/surface/stl.rs`, `src/surface/obj.rs` | STL (3D Systems, 1987, a de facto public spec) and Wavefront OBJ (also a published, uncopyrightable format) readers, for surface intake (SPEC-LIT §23) |
| `src/surface/classify.rs` | Column-parity ray-casting inside/outside classification (SPEC-LIT §23.3); Barill, Dickson, Schmidt, Levin & Jacobson, *ACM TOG* 37(4) (2018), the exact solid-angle winding number as tie-break |
| `src/surface/cutcell.rs`, the cut-cell section of `src/blockgen.rs` | Embedded-boundary volume/area fractions by supersampling (SPEC-LIT §24) — Aftosmis, Berger & Melton, *AIAA J.* 36(6) (1998) 952 for the castellation/cut-cell context; the cut face's closure-defined area vector, the merge rule and the sample count are *DESIGN* (§24.3, §24.5) |
| `src/restart.rs` | The `.mcr` restart file: full-precision `phi`, mesh-hash guarded, versioned. Format and layout are *DESIGN* — original, no external spec |
| `src/io/case_json.rs` | The JSONC case reader/lowering (`docs/05-io-redesign.md` §4.1). `schemars`-generated JSON Schema so the schema and the reader cannot disagree; the patch-major, ordered-array layout is prior art from `reference/pyfr`'s `[soln-bcs-<patch>]` INI convention (BSD-3), read as a case-format precedent, not code |
| `src/io/vtu.rs` | Appended-binary VTU/VTK writer; `reference/pyfr`'s BSD-3 `pyfr/writers/vtk/` was the cross-check for the encoding and offsets |
| `src/io/vdb.rs`, `src/io/nvdb.rs` | OpenVDB-compatible and NanoVDB volume writers, `fp32` and `fp16` (SPEC-LIT §45). The NanoVDB layout is read from the Apache-2.0 PNanoVDB/NanoVDB headers (AcademySoftwareFoundation GitHub); the `.vdb` layout, including the `_HalfFloat` save preference, from the Apache-2.0 OpenVDB source at `v13.0.0`, cited file by file inline |
| `src/io/output_plan.rs` | The `output` block of the case format, resolved and driven (SPEC-LIT §44) — ORIGINAL, no external spec |
| `src/io/usda.rs` | USD ASCII (`.usda`) scene writer referencing the VDB output — the format is Pixar's published USD spec |

## Ours by design

Marked *DESIGN* in `SPEC-LIT.md` where the literature does not prescribe the
choice. These are documented as our decisions in the code, not attributed to
anyone.

| Thing | Where | SPEC-LIT |
|---|---|---|
| The single Robin triple `(fr, ref_value, ref_grad)` for every BC | `src/field.rs`, `src/field_setup.rs` | §4 |
| Vertex welding by exact bit-equality only — no epsilon weld, because an epsilon is a silent geometry edit | `src/surface/mod.rs`, `src/surface/stl.rs` | §23.1 |
| Faces carved against solid cells are `wall` type and receive the same wall boundary conditions blockgen already writes, so a carved case runs unmodified | `src/blockgen.rs` | §23.4 |
| Gather over a cell→face CSR instead of scatter over faces | `src/mesh/topology.rs` and every kernel | §1 |
| Ordering a row of `A·psi` by the **global** face id rather than the local one, in one merged list with a two-bit flags word, so that the summation order is a property of the mesh and not of how the mesh was cut up. The identity map puts every internal face's key below every boundary face's, so on an undecomposed mesh the merged list is bit-for-bit the two old lists concatenated and no default can move — proved by construction, then measured (`ofgpu-validate` and every field the shipped cases write, unchanged to the byte). Applied to the four kernels that accumulate internal AND boundary faces into one register (`solAmul`, `lduAmul`, `lduRelax`, `lduSetValues`) and deliberately **not** to the 16 assembly and gradient kernels §70.5 enumerates, whose cut-face coefficients are computed by different arithmetic on the two sides and which an ordering therefore cannot fix. Costs ~52 B/cell, added and not recovered | `src/mesh/topology.rs`, `src/mesh.rs`, `cuda/ldu.cu`, `cuda/solver.cu` | §70 |
| Accumulating a cross-part reduction in INTEGER limbs against one GLOBAL anchor rather than combining per-part floating-point partials, so the answer is a function of the multiset of terms and not of the cut. `W = 30` and `K = 4` rather than the literature's `W = 40`, `K = 3`: at 30 bits a limb cannot overflow an `i64` for any mesh a 32-bit label can index, which deletes the carry-normalisation pass instead of scheduling one, and 120 binades is already 67 below the last bit a double can hold. The anchor is a GLOBAL maximum and is deliberately NOT fused into the limb collective - a per-part anchor truncates each part's terms at a different place and the partition invariance is gone, which is a **correction to the design note this section was written against**. Both constructions are implemented and measured side by side: the gathered per-part partial (run-invariant, four lines) moved in 20 of 108 relabelled decompositions on the solver's own output and 75 of 108 on an adversarial field, the exact accumulator in none. **Nothing calls it yet**, and §72.6's measured 4.1-5.0x is why that is a decision rather than a deferral | `src/exactsum.rs`, `cuda/exactsum.cu` | §72 |
| Running PCG and PBiCGStab over a decomposition by putting the halo exchange in **one place only** - immediately before the matrix product, which is the only operator in either recurrence whose stencil leaves a cell - and substituting the cross-part reduction for the local one, so that the recurrences themselves are the serial solver's kernels unchanged. Both invariance halves are made one predicate in code (`DistWorkspace::partition_invariant`) rather than a comment: the exact accumulator AND a preconditioner a cut cannot change. **The block-local DIC/DILU is named block Jacobi, not restricted additive Schwarz** - a correction to the design note this section was written against, since the halo is read by `lduAmul` and by nothing in `precon.rs`, so the overlap RAS restricts does not exist here (Saad 2003 ch. 14; Cai & Sarkis 1999 cited to be distinguished from). `normFactor`'s mean divides by the GLOBAL cell count, which closes the `sum(b)/n` hazard §72.7 left open. Measured rather than argued: the diagonal preconditioner degrades by exactly nothing at every part count, DIC/DILU by 0.91x-1.84x at P = 16 over the ten rows of five meshes (nine a penalty of 1.03x-1.84x, one a speed-up), and the degradation is a step function of which direction the cut crosses rather than a smooth function of P. Gated on the shipped cases at 78/78 bitwise-and-same-iteration under three partitioners, and at 156/156 again under every rotation of the part labels. **Nothing calls it yet** | `src/distsolve.rs` | §73 |
| Continuous blending across `y+_lam`, and the wall-adjacent-cell treatment | `src/wallfunctions.rs` | §6.4 |
| The skewness vector `s_f = Cf - (C_P + (1-w)d)` written from the interpolation WEIGHT rather than from a second projection, so the correction and the interpolation it corrects cannot disagree about where the uncorrected value was placed — plus a `1e-9 \|d\|` FLOOR below which the vector is zeroed. The floor was forced by a measurement, not chosen: `s_f` is a difference of two nearly equal COMPUTED positions, so when the true value is zero the computed one has no significant digits, and a uniform box came out at `6.9e-18` on some faces and exactly zero on others depending on its cell dimensions. Without it, "an unskewed mesh gives the same bits" would be a property of the numbers in the case file — this project's own first version of the test passed on one box and failed on another. Real skewness at a 2:1 interface is `0.1421`, eight orders above the floor; centroid noise on a mesh a thousand cells across is four orders below it | `src/mesh/geometry.rs`, `src/mesh.rs` | §2.5, §74.4 |
| Measuring the 2:1-interface convergence gate in **L-infinity as well as L2** — and the finding that L2 alone would have passed the UNMODIFIED code (1.985) while L-infinity shows it stalling dead (−0.070). A refinement interface is a two-dimensional set inside a three-dimensional mesh, so a volume-weighted norm gives a local defect there only its own share; the gate as originally briefed could have been met by doing nothing. This is the correction to the brief that mattered | `src/fv.rs` (`a_statically_refined_mesh_recovers_second_order`) | §74.5, §74.6 |
| The load-bearing piece at a 2:1 interface is the GRADIENT, not the snGrad scheme: Green-Gauss on the fine side is wrong at ZEROTH order (30.17 % of the gradient on the gate mesh, and it does not shrink with h), and that error is fed straight into §2.4's correction. `leastSquares`, which this crate already shipped, is exact for a linear field on ANY mesh and reaches L-infinity order 1.931 with no new code — recorded as the recommendation rather than buried, because it makes most of §74.4's second half optional | `src/fv.rs`, `cuda/fv.cu` | §74.6 |
| The skewness correction of the Green-Gauss gradient is a PICARD iteration, not a one-shot: one pass corrects the face value with a gradient that is itself uncorrected. Measured contraction 0.221, converging to `2.3e-15` on a linear field — stated with the ladder (0 / 1 / 2 / converged passes) rather than shipped as if one pass were exact | `src/fv.rs`, `cuda/fv.cu` | §74.4 |
| `ofgpu-validate` deliberately does NOT run `check_explicit_operators` on the refined mesh: two of its checks are FALSE there, by 30.17 % and 1.20 %, and that is what §74.4 exists to fix rather than a bug in them. The same two facts are asserted in `check_skew_correction` with their sign the right way round and their size printed, so the property is recorded rather than hidden by omission | `src/bin/validate.rs` | §74.10 |
| A 2:1-refined mesh EXCLUDED from the cuFFT direct Poisson path by name, with the exclusion measured — `pressure::cartesian::detect` is run on one and the sentence it prints is recorded, and the unrefined base grid of the same box is checked to still pass, so the refusal is about the refinement and not about the generator. No capacitance-matrix repair is offered: the trick §52.9 already refused needs O(1) modified rows and a refined block is O(N^(2/3)) of them | `src/pressure/cartesian.rs` | §74.8 |
| Bounding of `k` and `epsilon` | `src/turbulence.rs` | §6.1 |
| A driver may not default a MOMENTUM equation's convection entry to the `bounded` form. §3.1's correction subtracts `V_P (div u)_P` on the argument that `div u` is a convergence error; §25.1 makes it a prescribed constraint, so on a momentum equation it removes real converged momentum. Measured at -3.787% of the §32.5.2 force balance and closed to -0.000% by honouring the case's own entry — **and that measurement does not reproduce since §26.1**, which showed the dilatation it was integrating against to be itself an artefact of §25.1's `Q` being implemented without its conduction term (the same `bounded` run now closes the balance to +0.000%). The RULE stands: a fire plume's dilatation is real and the correction still eats it. On §26's energy equation the correction is applied unconditionally and the flag is documented as not being a setting - but NOT because it is physics there, which §26.1 measured false on the mass flux; because dropping it leaves the CONSERVATIVE form, which for an ideal gas at fixed `p0` is identically `(gamma/(gamma-1)) p0 div(u)` and carries no information about `T` (measured: `Nu` 128.5 against 71.7) | `src/momentum.rs`, `src/energy.rs`, `src/bin/fire.rs` | §3.1, §13.4.1, §26.1, §32.5.5 |
| `NO_WALL = 1e10` m in a domain with no wall in it, rather than a singular all-Neumann Poisson solve — which is also the physically right answer, since `F_1 → 0` and the van Driest length grows without bound | `src/walldistance.rs` | §6.6 |
| Reading the wall faces from the mesh's `PatchKind::Wall` rather than from any one field's `boundaryField`, since the wall distance is a property of the geometry | `src/walldistance.rs` | §6.6 |
| Cell extents measured as `2·max_f \|Cf − C\|` over the cell's own faces — exact for an axis-aligned hexahedron, and biased *down* (so `nu_t` down) on a skewed one, because the mesh does not retain the points a cell was built from | `cuda/les.cu` | §16.2, §16.3 |
| `y+` for van Driest damping from the LOCAL wall-normal shear, with the wall normal taken from `grad y` — no nearest-wall search, and van Driest's own reading of `A+` | `cuda/les.cu`, `src/les.rs` | §16.4 |
| Applying van Driest damping only where `y+ > 0`, so that a quiescent field or a wall-free box keeps its geometric filter width instead of having it annihilated | `cuda/les.cu` | §16.4 |
| Filter-width smoothing: ratio 1.15, two sweeps, off by default, raising rather than lowering — the same sweep as §13.2's local time step | `src/les.rs`, `cuda/les.cu` | §16.5 |
| The unstructured-mesh test filter for Deardorff — a face-neighbour gather that reduces to FDS's `(1, 2, 1)/4` kernel in one dimension, because an unstructured mesh has no diagonal neighbours | `cuda/les.cu` | §6.5 |
| `C_s = 0.168`, Lilly's inertial-range value, rather than a wall-tuned fit — with the reasoning, and the dictionary entry that overrides it, in the code | `src/models/les.rs` | §6.5 |
| `RasModel::Les` as a variant of the model enum, so a RANS-only driver refuses an LES case by name instead of reading it as laminar | `src/models/registry.rs` | §13.4 |
| Residual normalisation | `src/solver.rs` | §8.4 |
| Density-ratio buoyancy `b = g(T_ref/T − 1)` | `src/momentum.rs` | §9 |
| The over-relaxed `0.05·\|d\|` floor on the delta coefficient | `src/mesh/geometry.rs` | §2.4 |
| The limited-snGrad expression `min(1, α·\|orth\|/(\|corr\|+ε))` | `src/io/schemes.rs` | §12.3 |
| The LTS smoothing sweep and damping | `src/timescheme.rs` | §13.2 |
| `-permissive` and the loud-failure contract | `src/io/contract.rs` | §13.4 |
| Each field owning its own wall-function decision | `src/wallfunctions.rs` | §15.5 |
| The `wallTreatment` preset table (`standard`/`spalding`/`rough`/`lowRe`) and its precedence (explicit per-field type > per-patch override > case default) — one setting expands to per-field `BcKind`s at case-build time; the per-face kernel architecture of §4 never sees a preset, only what it expands to | `src/io/case.rs`, `src/blockgen.rs`, `src/field_setup.rs` | §29.1 |
| The consistency contract: the four (five, with `T`) per-field types on one wall patch must belong to one preset row — a mixed row is a §13.4 error naming the patch, the offending pair and the consistent completions; `-permissive` substitutes the row implied by the `nut` choice and says so | `src/field_setup.rs`, `src/io/contract.rs` | §29.1, §13.4 |
| `validate_low_re_wall_treatment`: a `lowRe` wall treatment is only valid under a turbulence model that itself integrates through the viscous sublayer — `LaunderSharmaKE` (§33) is the one model on that menu today, `kEpsilon`/`kOmega`/`kOmegaSST` still are not, so resolving `lowRe` under any of the latter three is a §13.4 error naming the menu and the alternative (`standard`); `-permissive` substitutes `standard` and says so. SPEC-LIT §32's own second finding — `cases/channelPeriodicFluxLowRe.jsonc` still diverged after its under-resolved side walls were corrected, because standard k-epsilon has no near-wall damping at all, not because the mesh was wrong | `src/io/case.rs`, `src/io/case_json.rs` | §32, §13.4 |
| The `grad(grad U)` route for the `E` term — the Gauss gradient of the ALREADY-COMPUTED cell velocity gradient (`RasCore::update_flow_derived`'s own `grad_u`), rather than a dedicated second-derivative stencil, boundary faces zero-gradient-extrapolated because `grad U` carries no boundary field of its own; SPEC-LIT §33.1 marks this *DESIGN* itself and states its cost (one more CSR gather, roughly three times a vector-gradient pass, paid once per outer iteration) | `src/turbulence.rs` (`ls_grad_grad_u_mag_sqr`), `cuda/turbulence.cu` (`turbLsGradGradUMagSqr`) | §33.1 |
| `MeshResolutionReport` — the §33.2 mesh check (worst wall-adjacent y+, cells globally below y+ 20) as a pure function of already-downloaded `k`/wall-distance/wall-face-owner arrays rather than a GPU kernel, so a driver can call it once at set-up without a new device dependency; counted GLOBALLY rather than per wall-normal column, a stated approximation (this crate carries no wall-normal-column topology) | `src/models/launder_sharma.rs` (`mesh_resolution_report`) | §33.2 |
| Reusing [`u_plus`]'s own exponential (Kader) blend for the thermal law `T+`, rather than the root-sum-square blend `epsilon`/`omega` use — the log branch is stated in terms of `u+` itself (§29.3), so sharing the blend makes the `Pr = Pr_t` identity exact rather than approximate | `src/wallfunctions.rs` | §29.3 |
| The thermal wall function rewrites the Robin triple as the `fr = 0` (fixedGradient) degenerate case, with `ref_grad` chosen so the total flux is exactly the analytic Jayatilleke `q_w` whatever `k_eff` the energy equation computed that face — the same lagged-coefficient convention every other wall quantity in this crate already runs at | `src/wallfunctions.rs`, `src/energy.rs` | §29.3 |
| Kays-Crawford's `Pr_t(Pe_t)` evaluated in the algebraically rearranged form `Pr_t = Pr_t_inf/(1/2 + h(u))`, `h(u) = (e^-u + u - 1)/u^2`, `u = 1/(C Pe_t sqrt(Pr_t_inf))` - the same function as the literature form, with both limits reduced to one line each and the large-`Pe_t` cancellation removed (the literature form is 3.6% wrong at `Pe_t = 1e8`); one branch at each end, at `Pe_t -> 0` because a resolved wall face hands it `Pe_t = 0` exactly and the direct form is NaN there | `src/energy.rs`, `cuda/energy.cu` | §37.2 |
| §37's `Pr_t` reaches §26's `k_eff` and nothing else. §29.3's Jayatilleke wall function keeps `Pr_t_inf` BY DERIVATION - its `Pr_t(u+ + P)` is the log-layer value, which is exactly Kays-Crawford's own `Pe_t -> inf` limit, and `P` already carries its own sublayer integral - which is what makes a wall-function mesh a CONTROL for the §32 experiment; §17's `G_b` keeps the constant too, and `KaysCrawford` with gravity on is therefore a §13.4 error rather than a silent second `Pr_t` | `src/energy.rs`, `src/bin/fire.rs` | §37.3, §29.3, §17 |
| `PrtModel` defaults to `constant`, and `Prt` is not renamed - under `KaysCrawford` the SAME entry is read as `Pr_t_inf` and the sublayer value is `2 Prt` by derivation rather than a second entry a case could set inconsistently. The default is frozen so that every measurement recorded before §37 stays reproducible without editing a case file | `src/energy.rs`, `src/io/case_json.rs` | §37.4 |
| The `C_3` default for buoyancy production | `src/turbulence.rs` | §17 |
| The inert-species choice for the sum-to-one constraint | `src/species.rs` | §19 |
| Geometric cell-set selection for sources | `src/sources.rs` | §18 |
| Zalesak limiter iteration count | `src/vof.rs` | §20.2 |
| `-permissive`, and the one-line-per-setting warning it prints | `src/io/contract.rs` | §13.4 |
| The three sub-blocks of `output` are three PURPOSES, and each takes only the formats that serve it — `visualisation` is `vdb`/`nvdb`, `exact` is `vtu`/`openfoam`, `restart` is always `.mcr`. A format from the wrong column is a §13.4 error that says which column it belongs in, because "ofgpu does not write vtu" would be a lie | `src/io/output_plan.rs` | §44.1 |
| `output.visualisation.precision` is the ONE place in the case format where reduced precision is legitimate. `output.exact.precision` and `output.restart.precision` exist as fields purely so they can be refused with a REASON rather than as "unknown field" — a lossy "exact" format is a contradiction in the name, and §5.1's argument for carrying `phi` across a restart rules out a rounded flux as much as a re-derived one | `src/io/output_plan.rs`, `src/io/case_json.rs` | §44.3 |
| `restart.keep: 0` means "keep every checkpoint", not "keep none" — the safe reading of a zero is the one that deletes nothing, and it is also the only way a case can ask for an unpruned series | `src/restart.rs` | §44.5 |
| `restart::Checkpoints` deletes only paths in its own `written` list — files *this* value returned from *its own* `write`. A `restart_*.mcr` left by an earlier run, or put there by a person, is never a candidate, because it was never in the list. A directory scan with a glob would delete it; this cannot | `src/restart.rs` | §44.5 |
| A case that carries the `output` block AND a command line naming `-output`/`-writeInterval`/`-restartWrite` is a §13.4 error naming both, rather than a silent winner. `ofgpu-fire`'s `run.endTime` disclosure line is the other pattern and is right THERE because the case's `endTime` still reaches §31.3's contract; nothing in `output` reaches anything else | `src/io/output_plan.rs`, `src/bin/{fire,k_epsilon}.rs` | §44.6 |
| A positive `output.*.interval` on a STEADY run is refused by name: those runs advance an iteration counter, not a clock. `interval` absent (or `0`) means "the final state, once" and runs everywhere | `src/io/output_plan.rs` | §44.4 |
| The device-memory report `ofgpu-fire` prints is a DIFFERENCE from the free memory the driver saw before the first field existed, not `total - free`. On a desktop card `total - free` is dominated by the compositor and the browser, which is not a property of the run; the difference is, and it is the only form in which two configurations can be compared on the same machine. Sampled at three points (after setup, after the first step, after the last) rather than every step, because nothing in the time loop allocates after the first iteration and a `cuMemGetInfo` per step would put a driver round trip and a stream synchronise inside the wall time the same table quotes | `src/bin/fire.rs` (`MemWatch`) | §65.4, §65.7 |
| `visualisation.fields` is checked TWICE — once before the time loop against the names the driver is about to build, and again inside `FieldSelection::apply` at every write. The early check is what stops a six-hour run failing at its first write; the late one is what makes the early list safe to trust | `src/io/output_plan.rs`, `src/bin/fire.rs` | §44.2 |
| `OutputPipeline` owns each stage's write COUNTER. `ofgpu-fire` passed a hard-coded `step: 0` and rebuilt its writers inside its write function, so every `.vtu`/`.vdb`/`.nvdb` of a run overwrote the last and the `.pvd`/`.usda` series never grew past one entry. A counter the pipeline increments, and writers built once, cannot be forgotten by the next driver | `src/io/output_plan.rs`, `src/bin/fire.rs` | §44.4 |
| The `.usda` scene's `filePath` extension follows the volume format actually selected instead of the hard-coded `"vdb"` — `-output nvdb,usda` used to write a scene pointing at files that do not exist | `src/io/output_plan.rs` | §44.1 |
| **Always** blending the two branches of the law of the wall — there is no `blended` switch, because there is no case in which the discontinuous form is wanted | `src/wallfunctions.rs`, `src/io/case.rs` | §6.4 |
| Evaluating the boundary conditions that read another field (`turbulentIntensity…`, `turbulentMixingLength…`, `totalPressure`, `pressureInletOutletVelocity`) once at setup, and refreshing only their value fraction from the flux thereafter | `src/field_setup.rs` | §4 |
| Defaulting `C_3` to the Henkes form, and spelling the override `C3Buoyancy` so it cannot collide with §6.1's dilatation `C_3` | `src/turbulence.rs`, `src/bin/buoyant.rs` | §17 |
| The `CoupledTurbulence` trait (`initialise`/`correct`/`nut`/`name`/`output_fields[_mut]`) - kept to exactly what a coupled driver's outer loop, writer seam and `.mcr` checkpoint need, and no more; a `dyn` dispatch is acceptable here (one virtual call per OUTER iteration) where it would not be in a standalone driver, because which model runs is a runtime fact read out of the case rather than known at the call site | `src/models/coupled.rs` | §30.2 |
| The LES wall-model preset mapping under `simulationType LES` - `standard`/`spalding` both read as `wernerWengleWallFunction`, `lowRe` as `nutLowReWallFunction` (`nu_t,w = 0`), and `rough` is a §13.4 error naming the two rather than an alias (a rough LES wall model is future work) | `src/io/case.rs` (`WallTreatment::les_nut_type`) | §30.1 |
| `ScalarBc::ThermalWallFunction { value }` - an explicit, per-patch `T_w` for `thermalWallFunction` on `T`, alongside the case-level `wallTreatment` auto-completion (which has no wall temperature of its own to write, only the neighbour cell's) - the same "explicit wall-function variant carries its own value" shape `TurbBc` already gives `nut`/`k`/`epsilon`/`omega` | `src/io/case_json.rs` | §29.1, §29.3 |
| The integrated wall-heat-flux report in `ofgpu-fire` - reading the SAME `(fr, ref_value, ref_grad)` Robin triple the energy matrix was assembled from, generically over every `wall`-kind patch, rather than re-deriving Jayatilleke or a plain Dirichlet flux externally per BC type | `src/bin/fire.rs` | §29.3, §30.3 |
| Keeping the STABLE branch of `G_b` in the `k` equation only, unless the case asks for it in `epsilon` as well | `cuda/turbulence.cu` | §17 |
| Selecting a source's cells geometrically — box, sphere, explicit list — and the `constant/fvSources` dictionary that expresses it | `src/sources.rs` | §18 |
| Refusing a source whose selection catches no cells | `src/sources.rs` | §18, §13.4 |
| Choosing the inert species by the largest volume-weighted mean when the case names none | `src/species.rs` | §19 |
| Writing `phi` at round-trip precision rather than at `writePrecision`, because its value carries a discrete constraint and not just a number | `src/io/fields.rs` | §5.1, §22 |
| Three iterations of the Zalesak limiter, and the GLOBAL bounds `[0, 1]` rather than the local-extremum ones | `cuda/vof.cu` | §20.2 |
| `eps = 1e-8/L` in the interface-normal normalisation, with `L` the cube root of the mean cell volume | `src/vof.rs` | §20.1 |
| A ninety-degree contact angle at every non-cyclic boundary — the one choice that adds no unstated physics, since §20 specifies no contact-angle model | `cuda/vof.cu` | §20.4 |
| Forming the compression term as `alpha_f(1 - alpha_f)` from the interpolated face value rather than interpolating the cell product, which would switch compression off exactly at a sharp interface | `cuda/vof.cu` | §20.1 |
| The plain interpolated cell gradient as the face interface normal, rather than that gradient with its normal component replaced by `snGrad(alpha)` — measured, and the replacement is seven times worse | `cuda/vof.cu` | §20.4 |
| Leaving the momentum predictor switchable, and taking `\|U\|` as the limiter sensor for `div(rhoPhi, U)` | `src/vof.rs` | §20.3, §5.4 |
| `rho_f` by linear interpolation of cell `rho`, `rho_infinity` from `TRef` at `p0` | `src/energy.rs` | §25.3 |
| `chi_r` radiant-fraction floor default (0.35, FDS practice) and the `LES` mixing-frequency substitute `C_EDM'` defaulted to the same `4.0` as the RANS constant until a case overrides it | `src/combustion.rs` | §27 |
| Combustion applied as its own operator-split pass over `Species`, not fused into its transport matrix — an EDM rate is a LOCAL field, not the uniform value every other `SourceSet` entry is | `src/combustion.rs` | §27, §18 |
| The fuel sink's backward-Euler closed form on the 0-D reaction ODE, positive for any `dt` | `src/combustion.rs` | §3.4, §27 |
| `s×s` face supersample lattice default `s = 16`, and `theta_min = 0.2` for cut-cell merging | `src/surface/cutcell.rs` | §24.2, §24.5 |
| Cut face centroid as the mean of interface sample midpoints, and its patch from the nearest surface triangle | `src/surface/cutcell.rs` | §24.3 |
| `ofgpu-fire`'s `Y_O2`/`Y_P` boundary conditions derived from patch kind (ambient `inletOutlet` on `open`, `fixedValue 0` on a fuel `inlet`) rather than named per case — only `Y_F` is a case-supplied field | `src/io/case_json.rs`, `src/bin/fire.rs` | §27, §19 |
| Species transport and combustion/radiation's segregated lag: read at the END of the PREVIOUS unit of work, registered before the CURRENT iteration's target-divergence/energy assembly — the same one-iteration lag `nu_t` and `T` already run at | `src/bin/fire.rs` | §25, §26, §27, §28 |
| The `.mcr` restart format itself (header, mesh hash, versioning, full-`f64` `phi`) | `src/restart.rs` | — |
| The cyclic-pair format itself — naming both sides of a pair plus the transform (`translate` only; `rotate` is a named §13.4 refusal, not a silent subset), rather than OpenFOAM's own separate `neighbourPatch`/`transform` dictionary entries on each side independently | `src/blockgen.rs` (`-cyclic x\|y\|z`, `BlockSpec::set_cyclic_axis`), `src/io/case_json.rs` (`mesh.cyclic[]`) | §31.1 |
| Face matching by nearest translated centroid, and the two invariants that make a mismatched pair loud instead of silently unconserving — every face matches exactly once (a bijection), and `Sf_a == -Sf_b` after the transform to a stated tolerance | `src/blockgen.rs` | §31.1 |
| The transient/algorithm contract — a case's `run.endTime` and `numerics.ddt`/`numerics.algorithm.kind` are checked as ONE combined setting (`is_transient_run`, `check_transient_algorithm_contract`) rather than three independently-valid ones, because that is exactly how `cases/burnerPlume.jsonc` reached step 20 as `Inf` with nothing having warned | `src/io/case.rs` | §31.3 |
| A `sources[]` `momentumSource` entry in JSONC — the one case a PERIODIC domain needs (no inlet to prescribe a mass flow from) reusing `crate::sources::SourceTerm::BodyForce`/`CellSelector::All` verbatim, rather than a second JSON copy of the whole `constant/fvSources` box/sphere/six-term-kind surface that format already has | `src/io/case_json.rs` (`JsonSource`) | §18, §31.1 |
| `empty`/`symmetry` as JSONC `patches[]` kinds, enforced as CONSTRAINTS rather than boundary conditions: a rule of either kind may not also name a per-field BC (`U`, `p`, `T`, `Y_F`, `k`, `epsilon`, `omega`, `nut`, `treatment`, `Ks`, `Cs`) — a §13.4 error naming the offending field — and `empty` is refused (naming the slot and its actual cell count) on any axis with more than one cell, the same "single cell across" rule `blockgen`'s own canned cases already enforce | `src/io/case_json.rs` (`validate_constraint_rules`, the `PatchPresetKind::Empty` cell-count check in `build_block`) | §34.1 |
| Generalising `BlockSpec::cyclic` from one axis to a list, with the pairing rules enforced axis-by-axis rather than case-wide: an axis claimed by two `mesh.cyclic` pairs is a §13.4 error naming the axis (`cyclic_slot_set` in JSONC, `set_cyclic_axis` in `BlockSpec` as the second, `BlockSpec`-flavoured line of defense) — "a patch may appear in at most one pair" falls out of this for free once pairing is axis-based, since two pairs can only collide by naming the same axis. A cyclic pair and an `empty`/`symmetry` constraint rule naming the same slot is a separate §13.4 error naming both, because the two are contradictory statements about the same faces | `src/blockgen.rs` (`BlockSpec::cyclic: Vec<usize>`, `set_cyclic_axis`), `src/io/case_json.rs` (`cyclic_slot_set`, `cyclic_axis_of`) | §34.2 |
| The bulk-temperature thermostat itself: a proportional controller on the domain's VOLUME mean `T` (`q = -rho_cp (T_mean - T_target)/tau`, uniform, `rho_cp` fixed at `rho(T_target)*cp` rather than recomputed from the current `T_mean` every iteration — a genuine constant gain for the whole run, and one that does not change WHERE the controller settles, only how fast). SPEC-LIT §35.1 marks the whole mechanism *DESIGN* — the literature gives no standard fix for a periodic, all-Neumann energy equation's free additive constant, only the analogy to §8.5's pressure null space | `src/sources.rs` (`Thermostat`, `SourceTerm::Thermostat`) | §35.1 |
| `tau`'s default, the domain's own flow-through time `V^(1/3)/U_ref` — a direction-free length scale (this project's meshes are not all axis-aligned channels) times the volume-mean speed of the case's own initial condition, rather than a streamwise length that would need an axis to be given | `src/sources.rs` (`flow_through_time`), `src/bin/fire.rs` | §35.1 |
| The saturation ceiling on the controller's own output (a `T_target` unreachable against a competing Dirichlet wall must not make this term grow without bound) — `1000 K` of implied swing per `tau`, an order of magnitude past any gap this project's own cases produce, and `warn_once`'d when it engages | `src/sources.rs` (`THERMOSTAT_SATURATION_DELTA_T`) | §35.2 |
| A `thermostat` restricted to `selection: "all"` (or omitted) only — refused by name if a case asks for a zoned one, since a sub-zone's mean is not the domain-wide null direction this term exists to remove | `src/sources.rs` (`read_sources`), `src/io/case_json.rs` (`JsonSource::Thermostat`) | §35.1 |
| The §35.1 warning's own test: reusing `Simple::pressure_is_pinned` (already SPEC-LIT §8.5's "does this pressure Poisson problem have a Dirichlet anywhere" check) as "is this domain closed", and a new, symmetric `GpuScalarField::has_a_dirichlet` for "is `T` Dirichlet nowhere" — a WARNING, not an error, since a transient run of the same domain is legitimate | `src/bin/fire.rs`, `src/field.rs` (`GpuScalarField::has_a_dirichlet`) | §35.1, §13.4 |
| One reader that answers a driver's OWN equations' scheme, relaxation and linear-solver entries for BOTH case formats (`CaseNumerics`), per equation and by that equation's own key, rather than a private copy of the lookups in each driver — the fourth instance of the same silent-discard defect is what made a shared seam the fix instead of a fourth private reader. `CaseControls` stops at the turbulence equations by design, and on the JSONC path has no `fvSchemes` at all, so `div(phi,U)`/`div(phi,T)`/`solvers/T` had no cross-format home before this | `src/bin/common/mod.rs` (`CaseNumerics`), `src/io/case_json.rs` (`LoweredCase::solver_for`, `relax_for`, `div_named`), `src/io/case.rs` (`relaxation_factor` made public) | §13.4.1 |
| A control initialiser a case feeds is written out field by field, with no `..Default::default()` tail, so the next field added to `MomentumControls`/`EnergyControls` is a compile error at that site rather than a default nobody reviewed | `src/bin/fire.rs` (`fire_controls`) | §13.4.1 |
| The standing "two runs must differ" test: two runs of the driver's OWN `run`, on generated case files identical but for one setting, must write different fields — with exactly one admissible exception (a setting whose effect is identically zero on every mesh the format can build, `snGradSchemes` on a Cartesian box), which is asserted on the control struct instead and says why | `src/bin/fire.rs` (`every_wired_setting_changes_what_the_run_writes`, `laplacian_sn_grad_reaches_both_momentum_and_energy`) | §13.4.1 |
| `div(phi,Y_F)` honoured only when the case NAMES it, with no fall back to `divSchemes/default` — the species equations' SPEC-LIT §19 default is bounded upwind and every case default in this tree is unbounded `Gauss upwind`, so falling back would unbound an equation the case never mentioned | `src/bin/fire.rs`, `src/bin/common/mod.rs` (`CaseNumerics::div_named`) | §19, §13.4.1 |
| `ofgpu-fire`'s two §13.4 refusals of its own: `nOuterCorrectors > 1` (it advances energy/species/radiation/momentum-pressure once per step and has no PIMPLE outer loop) and `residualControl` (its loop counts iterations and consults no residual — wiring it through `Simple::set_residual_control` would produce a setting that is accepted and still inert, which is the defect rather than the fix). Both name what IS available | `src/bin/fire.rs` (`check_outer_correctors`, `fire_controls`) | §13.4, §14 |
| The run-mode conflict rule: `ofgpu-fire` takes steady-vs-transient from `-endTime`/`-deltaT`, so a case naming a transient `ddt` with no `-endTime` is a §13.4 error naming both, while `steadyState` under `-endTime` is promoted to `Euler` (`DdtScheme::reconciled`'s own rule) and PRINTED. A promotion nobody sees is a silent substitution | `src/bin/fire.rs` (`resolve_ddt`) | §13.4, §31.3 |
| The SWEEP rule that follows a fixed unread-setting defect: every published measurement the affected driver produced is RERUN or explicitly MARKED as pre-fix, and a stale number left standing without a marker is treated as the same defect one step downstream. Applied across both READMEs, `docs/07-fire-solver.md`, `cases/README.md`, every `cases/*.jsonc` header, `SPEC-LIT.md` and the replayed constants in `validate.rs` | `SPEC-LIT.md` §13.4.3 (the rule); `docs/07-fire-solver.md` §3, §6; `cases/burnerPlume*.jsonc` | §13.4.3 |
| A comparison between two MODELS run under the same discarded settings (P1 against fvDOM on one case) is named as the one class of claim an unread-setting defect does not invalidate — stated explicitly rather than left for a reader to work out, and the rerun is reported anyway so the statement is checkable | `SPEC-LIT.md` §13.4.3, §36.7; `docs/07-fire-solver.md` §3 | §13.4.3, §36.7 |
| The standing "two runs must differ" test given to EVERY driver, not only the one whose defect prompted it — six of them now, each running that driver's own `parse` + `run` on a `blockgen::write_case` case with one dictionary entry edited, comparing every field file written. Three pieces of the shared harness exist because of ways such a test can pass while measuring nothing: `Knob::apply` asserts the text it replaces is still in the generator (a drifted knob would turn nothing and the pair would compare two identical runs), `Knob::pre` applies an ENABLING edit to both sides (`gradSchemes` is read by a limited/deferred convection scheme and by nothing else, so turning it alone under first-order upwind is inert by arithmetic), and `written_time_dirs` compares only the time directories (comparing the case root would compare the knob's own edit with itself) | `src/bin/common/mod.rs` (`knobs`), `src/bin/{plume,buoyant,vof,k_epsilon,k_omega}.rs` | §13.4.1 |
| Refusal, not a printed note, for a case-format block NO driver implements — one shared checker every JSONC-reading driver calls, because a note is per driver and `ofgpu-fire` printed one for `output` while `ofgpu-k-epsilon`, reading the same format, printed nothing. Half-honouring was the alternative and is worse: three of the block's knobs (`visualisation.fields`, `visualisation.precision`, `restart.keep`) have no implementation anywhere in the crate, so wiring `format`/`interval` and dropping the rest would manufacture §13.4.1's defect inside its own fix | `src/bin/common/mod.rs` (`refuse_unimplemented_blocks`), `src/bin/fire.rs`, `src/bin/k_epsilon.rs`, `docs/case-example.json`, `docs/schema/case-1.json` | §13.4.2 |
| `RunControl::max_co` made an `Option<Scalar>` rather than a `Scalar` defaulted to zero: "the case did not say" and "the case said zero" are different states, and a §13.4 refusal has to tell them apart. It was written by the lowering and read by nobody at all before this | `src/io/case_json.rs` (`RunControl`) | §13.4.2 |
| Gravity refused to a driver with no temperature to build §17's `G_b` from (`ofgpu-k-epsilon`, `ofgpu-k-omega`) — both models have carried a `set_buoyancy` all along that nothing called. The refusal fires only where the CASE named gravity (`constant/g` present, or the JSONC path, where `physics.gravity` is required), because `BuoyancyCoeffs::default()` is `(0 0 −9.81)` and refusing on `is_active()` alone would refuse every case in this repository over a number no case file contains | `src/bin/common/mod.rs` (`refuse_buoyancy_without_temperature`) | §13.4, §17 |
| `nNonOrthogonalCorrectors` refused ONLY where it reaches no equation at all. No turbulence model in this crate loops over it — `energy.rs`, `momentum.rs`, `scalar_transport.rs` and `simple.rs` each do — so it is inert in `ofgpu-k-epsilon`/`ofgpu-k-omega` and honoured in `ofgpu-plume`/`ofgpu-buoyant`/`ofgpu-fire` through their `T`/`U`/`p` equations. A blanket refusal would tell a user "not supported" about a setting three of their four equations honour | `src/bin/common/mod.rs` (`refuse_non_orth_correctors_without_another_equation`) | §13.4, §3.2 |
| The one dissipation slot `epsilon` and `omega` share is filled by the entry the MODEL names, not the entry the dictionary happens to carry. The old rule's comment said "epsilon and omega never coexist"; `blockgen::write_case` writes both into every case it generates so one directory runs under either driver, and `cases/channelKW` carries both — so a k-omega run took `div(phi,epsilon)`, `solvers/epsilon` and `relaxationFactors/equations/epsilon` for its OMEGA equation. Both case formats route through one function | `src/io/case.rs` (`dissipation_key`, `dissipation_from_model`), `src/io/case_json.rs` | §13.4.1 |
| `ofgpu-vof` honours `controlDict`'s `adjustTimeStep`/`maxCo`/`maxDeltaT` while every other driver REFUSES `adjustTimeStep yes` — the asymmetry is the point: `ofgpu-vof` is the one driver in this crate whose step is adaptive, so it is the one place the entries mean something, and the refusal for everyone else lives in `read_control_dict`, once | `src/vof.rs` (`VofControls::from_case`), `src/bin/vof.rs`, `src/io/case.rs` (`read_control_dict`) | §13.4, §20.2 |
| A `bounded` prefix on `div(rhoPhi,U)` refused even though the substitution would be CORRECT: §20.3's conservative form must not subtract the face sum of `rhoPhi`, which is `−(ρ − ρ⁰)V/Δt` exactly and is the other half of `d(ρψ)/dt`. Being right is not a licence to be silent — the case is asking for an equation that is neither conservative nor non-conservative and has to be told | `src/vof.rs` (`VofControls::from_case`) | §13.4, §20.3 |
| A retired case that a LATER §13.4 rule has made unrunnable is MOVED OUT of the case set, not resurrected and not left in place: `channelThermalLowRe`/`channelPeriodicLowRe` name `lowRe` with `kEpsilon`, which §33's rule (added after their runs) refuses. Editing them to `LaunderSharmaKE` would make the file no longer the file its recorded numbers came from, so the files are kept as dated records under `cases/retired/` — with a README naming the live successor for each — and every published command that would fail is removed rather than commented out. A published command that fails is worse than an absent one | `cases/retired/channelThermalLowRe.jsonc`, `cases/retired/channelPeriodicLowRe.jsonc`, `cases/retired/README.md`, `cases/README.md` | §13.4.3, §33 |
| The CUDA-graph guarantee is checked per module, not asserted once. §81's registry derives its population from disk — every `.rs` under `src/` outside `src/bin/` that launches a kernel or declares a per-iteration entry — and requires the table to equal it in both directions, so a module added tomorrow is gated, excused by name, or it stops the tests. 50 modules: **36 gated, 10 refused by name, 1 outside the iteration, 3 ungated** against a ceiling that only falls. Writing the gates found six modules that could not be captured; three were the same defect (a *diagnostic counter* reduced on the device and read back to the host once per iteration) and are fixed behind flags that default to on, so every existing run reports what it reported before; three are refused with the reason and the alternative, and the refusals are **executed** rather than asserted | `src/capture/`, `src/device.rs`, `src/wsgg.rs`, `src/combustion.rs`, `src/s2s.rs` | §81 |
| Every source file's provenance declaration is checked in BOTH directions — the file names its category and carries "No GPL-licensed source was consulted", and `PROVENANCE.md` names the file (directly or through a wildcard row such as `src/bin/*`) — so an acquirer auditing file by file and one auditing the table reach the same answer. 181/181 either way, and **enforced by test**: `provenance_audit::every_source_file_declares_its_provenance` fails naming any file missing either line, and `provenance_audit::notice_and_provenance_quote_the_real_file_count` fails if this document or `NOTICE` stops quoting the real count. It was a prose claim until it was found to be false — 85 of 105 files carried the line while both documents said 105 | all of `rust/src`, `rust/cuda`, `rust/tests`, `rust/build.rs`; `rust/src/lib.rs` (the test); this file | §0, `LICENSING.md` §6 |
| An evaporating spray's energy coupling is the convective heat plus the arriving mass's SENSIBLE enthalpy, and **not** a second latent-heat sink. The droplet's own budget `Q_c = C dT_p + dm h_v` (76.10) already puts every joule the phase change consumed inside the convective term §68 deposits, so `q_lat` deposited again is the same energy twice — measured at 0.398 of the convective heat on a spray still warming, approaching one at the wet bulb. §76.14's refusal of energy coupling was right and its stated reason was half a step off; both are corrected here | `src/parcels/couple.rs`, `cuda/parcelcouple.cu` | §77.4, §76.14 |
| The ENERGY half of a phase change's effect on §25.1's divergence arrives through `Q` with no code at all — `energyTargetDivergence` reads `EnergySources::q` — while the VOLUME the added mass occupies is not a heat source, cannot be written as one, and has to be handed over. §26.1 records `Q` running for rounds without its conduction term; this is the same asymmetry with the halves swapped, and it is stated and tested separately rather than assumed. It is added by a NEW method that calls the old one and then accumulates, so `energyTargetDivergence` and `update_target_divergence` are untouched and a run with no dispersed phase is bitwise what it was by construction | `src/energy.rs` (`update_target_divergence_with`), `src/parcels/couple.rs` | §77.6, §25.1, §26.1 |
| Neither §77 source is an implicit sink. `mdot` is not sign-definite — a droplet in supersaturated air grows — so `-mdot cp` and `-mdot(1-Y)/rho` would need a `max(mdot, 0)`, which would be the first clamp in this three-section addition and would cost §68's "Patankar by construction, no sign branch" guarantee. Both are explicit and the reason is recorded | `cuda/parcelcouple.cu` | §77.5, §68.6 |
| §77 was committed with its gates written and never run, and re-measuring them found nothing wrong with the physics and two things wrong with the writing: every `(77.x)` in the code pointed at the wrong equation, because the spec's equations were reordered after the comments were written and nothing checks a cross-reference; and a kernel comment claimed a host-side `rho > 0` check that does not exist. A third seam is named rather than closed — (77.2)'s `cp_g` is §66's film property and the equation it feeds divides by §26's, and nothing joins them | `src/parcels/couple.rs`, `cuda/parcelcouple.cu`, `src/parcels/couple/tests.rs`, `src/bin/validate.rs` | §77.13, §77.12 |
| `mass evaporation` carries all three couplings at once and there is no way to ask for one. A case that took the vapour and left the enthalpy, or the enthalpy and left the volume, would have a run whose mass or energy does not close and no way to find out; the four illegal combinations are refused by name at setup | `src/parcels/couple.rs` (`ParcelCoupling::new`) | §77.7, §13.4 |
| `inletOutlet`'s `inletValue` is an entry §13.4.1 cannot see, because whether the solver reads it is decided by the FLOW and not by the document: it is read on exactly the faces where `phi_b < 0`, so on a channel that never backflows any number there produces the same field. Refusing the entry would be wrong - it is exactly what a recirculating outlet needs - so the run REPORTS how many outlet faces the flow re-entered through, and a run reporting zero has said in its own output that the entry could not have moved that answer. The pair test is written in both halves: bitwise identical forward, and moving by more than `1 K` with the inlet velocity reversed | `src/cht/flow.rs` (`OpeningReport::n_backflow`), `src/bin/cht.rs` | §79.5, §79.11, §70.7 |
| A reported WALL temperature is the evaluated FACE value and not the adjacent cell's, and on a fixed-flux patch the two differ by exactly `q"(dz/2)/k_s` - `0.098 K` on the coarsest micro-channel mesh, `0.0011` in `R_t`. That is `1.2 %` of `R_t,in` and LARGER than the level-to-level mesh change it would have been read against, so a gate reduced from cell values would have carried a systematic offset the size of a refinement level. The first reduction of Gate 6 did exactly that | `src/bin/validate.rs` (`qm_top_wall`), `cases/quMudawar.cht.jsonc` | §79.12, (79.14) |
| Every `(79.x)` and `§79.x` reference in §79's inherited code pointed at a subsection layout that did not exist. The driver and the reader were written and committed before the specification section was, so `§79.3` meant the flux-establishment pass in one file and the outflow treatment in another, `§79.4` meant `inletOutlet`, and `§79.5` meant `buoyancy`. All 30-odd sites were re-checked by hand against the section as written - including the ones inside user-facing error messages. This is the class §77.13 named and §78's numbering convention was written to head off, and nothing in this repository checks a cross-reference | `src/cht/flow.rs`, `src/io/case_cht.rs`, `src/cht.rs`, `src/bin/cht.rs`, `src/bin/validate.rs`, `cases/quMudawar.cht.jsonc` | §79.13, §77.13 |
| §60.2's list of what an opening would need was right about four things and loose about one word: an *outflow non-reflecting treatment* is a compressible solver's problem, and this fluid side is incompressible - the pressure equation is elliptic and there is no acoustic mode to reflect. The whole outflow treatment is `momFluxIsPrescribed` returning false at an `fr = 0` velocity patch | `src/cht/flow.rs` | §79.3 |
| Backflow through an outlet is not exotic. Blowing `0.05 m/s` up through a buoyant cavity whose own circulation is an order of magnitude stronger makes the flow re-enter through **7 of the outlet's 10 faces**, with the global mass balance still closing to the pressure solver's tolerance because the outlet flux is made of both signs. The test asserted `n_backflow == 0` first, on the reasoning that the outlet is downstream of the inlet, and failed | `src/cht/flow/tests.rs` (`a_closed_cavity_and_an_open_one_differ_only_by_the_option`) | §79.5 |
| While the flow leaves, `inletOutlet` is `zeroGradient` in EVERY BIT - `fr = 0` and `refGrad = 0` make the switched triple `T_b = T_P` through the same `fldMixed` arithmetic - which is what makes it safe to sweep the value-fraction kernel over the whole CONCATENATED boundary of §47.4's thermal mesh rather than over the outlet's own face range. Asserted as equality, in the cells and in the evaluated face values | `src/io/case_cht/tests.rs` (`inlet_outlet_is_bitwise_zero_gradient_while_the_flow_leaves`) | §79.5 |
| A micro-channel unit cell is a box with a rectangular hole in it and `blockgen` builds boxes, so it is NINE regions and twelve conformal couples. Eight of the twelve are silicon against silicon and are not an approximation: at `R_c = 0` on a matched orthogonal mesh the series conductance `h_G = k/(2h)` gives exactly §3.2's internal-face coefficient `k |Sf|/(2h)`, so the cut is a renumbering. The claim is ROUND-OFF and not bitwise - the two forms round to `k` within an ulp but not necessarily the same ulp, and the two systems are solved by different Krylov iterations because the coupled entries live in `boundary_coeffs` | `src/io/case_cht/tests.rs` (`the_nine_box_decomposition_is_the_single_box_it_was_cut_from`), `cases/quMudawar.cht.jsonc` | §79.8 |
| A `fallback_name` handed to `contract::unsupported` is a PROMISE printed in the non-permissive message ("run with -permissive to substitute X and continue"), so a refusal that also returns `Err` unconditionally must not name a real substitute. §79's patch-`kind` refusal named `wall`, which would have told a user that `-permissive` turns a mistyped `inlet` into a no-slip wall when in fact the case is refused either way | `src/io/case_cht.rs` | §79.10, §13.4 |
| A short heated duct's energy balance has THREE terms and the obvious reading has two: the inlet's `fixedValue` temperature conducts heat back OUT through the inlet face, `0.3 %` of the heater on the 4 mm test rig. The first version of the balance gate left it out and had to be given a `1e-3` tolerance to pass; with it in, the same gate holds at `1e-5` | `src/io/case_cht/tests.rs` (`the_openings_close_the_global_balance_and_the_bulk_rise_is_the_identity`) | §79.7 |
| The refusal of `stick` and the refusal of `film` were ONE sentence and are two different things. §66.10 refused `stick`, `spread` and `film` together with *a wall film is needed to receive the mass*; that is wrong about `stick`, because receiving the mass needs a flag and a rule about the velocity, and §66's pool already holds everything else — it reclaims no slot, so the arriving droplet's diameter and weight are still there. The film is needed for what the mass does NEXT. Separating the two is what made §78 small, and the three refusals now carry three different notes | `src/parcels.rs` (`WallAction::from_name`) | §78.11, §66.10 |
| The wall ledger is a PARTITION of the pool, not an accumulator: `live` / `deposited` / `gone` are disjoint and cover every slot by construction, so "nothing vanishes at a wall" is a statement about BITS — the mass on the wall is bitwise the mass that arrived, because nothing writes `d` or `n_p` at a deposit — rather than a tolerance. No atomic, floating-point or otherwise; it is a host-side gather over a snapshot the driver already downloads | `src/parcels.rs` (`ParcelSnapshot::deposited`/`gone`/`pool_mass`) | §78.6, §78.9 |
| Mundo's splash criterion is decided in its FOURTH POWER. `K = Oh Re^1.25` is never formed; `K^4 = We^2 Re` is, because `Oh Re = sqrt(We)` exactly and `x -> x^4` is strictly increasing on the non-negatives. The same decision, with no `pow` and no `sqrt` — which matters because §38.6 records that `pow` with a non-integer exponent is not bit-stable across compute capabilities, and a regime map that flips on the hardware is a different model on the hardware. Bai & Gosman's `La^(-0.18)` cannot be written this way, and that asymmetry is why `mundo` is the default | `cuda/parcels.cu` (`ofpImpactRegime`), `src/parcels/impact.rs` | §78.4, §38.6 |
| The design note's splash parameter is not Mundo's. The note gives `K = Oh^(-1/2) We^(3/4)` against `K_crit ~ 57.7`; Mundo's is `K = Oh Re^1.25`, and substituting `Oh = sqrt(We)/Re` shows the two differ by `Re^(1/4)` — a factor of 5.5 at the `Re ~ 900` of a 100 um water droplet at 9 m/s, where the note's expression returns 316 against its own stated threshold of 57.7. The implementation follows the paper | `src/parcels/impact.rs` | §78.13 |
| Splash is DETECTED and not enacted, and the consequence is published rather than hidden: the parent deposits whole, `n_splash` is a first-class counter, and the wall deposit is stated as an upper bound by that many impacts. Bai & Gosman's own retained-mass fraction is declined for two reasons — it is stochastic and this crate has no RNG (§66.8), and deleting the un-retained part would break the exact partition the section exists for | `cuda/parcels.cu`, `src/parcels.rs` (`ParcelStats::n_splash`) | §78.7 |
| A rule that condemned every `§NN.M` in a source comment that is not in `SPEC-LIT.md` would have "fixed" **171 correct citations** — Patankar §4.2, Jasak §3.4.3, Saad §6.7/§12.4, Ferziger & Perić §7.5, `docs/07-fire-solver.md` §1.1. §80's audit therefore attributes a citation before it checks it: it belongs to the last document named on its own line or the line before, and to `SPEC-LIT.md` otherwise. The window width was measured, not chosen — one line excuses 135 and flags 47, two lines excuse 203 and flag 19, the whole comment block excuses 307 and starts swallowing real SPEC-LIT citations under a module header that named ASHRAE once |
| `cuda/fv.cu` cited `(S3.3)` and `(S3.5)` for Jasak's Green-Gauss gradient and TVD ratio. **Both resolve against `SPEC-LIT.md`** — as §3.3 "Temporal" and §3.5 "Gradients" — so an existence check passes them for ever while they point at the wrong document. Only naming the work on the citation's own line makes them right, and only reading the bibliography entry made them visible: this is the class of defect §80.8 states it cannot automate away |
| The §77 recurrence is a spelling problem, and it is now counted: **252 symbols in `SPEC-LIT.md` are BOTH an equation label and a subsection number**, and **972 source citations use the bare `SNN.M` form on one of them**, which does not say which is meant. That is how one token for 77.5 came to mean §77.4 in one file, §77.6 in another and §77.5 in a third. §80.4 ratchets the count: it may fall, it may not rise, and new code writes `§NN.M` or `(NN.M)` — or, in an ASCII-only file, `(SPEC-LIT NN.M)` or `(SNN.M)` — each of which resolves to exactly one thing |
| Two semantic rules were measured against the tree and REJECTED rather than shipped weakened: requiring a possessive gloss (``§NN.M's `word` ``) to occur in that subsection's own body matches only **81.1 %** of 872 sites, and requiring a widely-cited symbol to quote a distinctive word of its own heading matches **78.4 %** of 241 — the misses are headings like "Validation" and "Discretisation" that have no distinctive word. A gate with a 19 % false-positive rate is a gate that gets deleted |
| A fenced line ending in `(NN.M)` is not necessarily a label. §80's parser applied "a reference does not define the symbol it cites" to prose only, so §77's validation row `cooling / expansion   17.59   (17.6)` — a MEASURED VALUE beside a computed one — defined an equation `(17.6)` for §17, a section with no subsections and no equations of any kind. `S17.6` would have resolved against a symbol that does not exist, and §17 entered the set of sections that label equations, making §80.2's own sentence ("twenty-nine, all §40 or later") true of the document and false of the code enforcing it, which counted thirty. A label now defines only inside a fenced block OF ITS OWN SECTION: 302 labels become 301, thirty sections become twenty-nine, and the sentence is true of both |
| The distinction between a `work` (excused) and a `document` (checked) held only while the path did. Both took the same branch when the file could not be read, so hiding `docs/07-fire-solver.md` turned a deliberately broken citation into it from a failure into a SILENT PASS — one `git mv` from a promise that had stopped being kept with nothing said. A missing `document` now fails its citations, and a test fails first and names the file |
| An ambiguous form is worth removing before it is worth counting. §80.4 ratcheted the bare `SNN.M` and left `(SNN.M)` — 357 citations — bounded by nothing, so the ledger's stated purpose ("the next file to add one fails the build") was false for a quarter of the population. Resolving `(SNN.M)` exactly like `(NN.M)` costs EIGHT corrections in the whole tree, every one a subsection written in the parenthesised spelling and one of them inside a user-facing refusal message. The same tightening applied to the bare form fails 586 sites, which is why that one is ratcheted and this one is not. The replacement spelling is form B, `(SPEC-LIT 78.7)`, because all five files are ASCII-only — no `.cu` file in this tree contains a single `§` |
| A rule loose in one direction manufactures false findings as readily as it excuses true ones, and §80 printed one of each. Its own "at least one real dangling reference inside `SPEC-LIT.md`" was `(S36.1)` in §62.6, argued from §36 labelling no equations. §36 labels none — which is exactly the case where the parenthesised form resolves against HEADINGS, and §36.1 is a heading. The citation was correct all along |

## GPU plumbing and tooling — original

No external source. These have no counterpart in any CFD code we are aware of,
and they are a larger share of the value than their line counts suggest.

| File | What it is |
|---|---|
| `src/device.rs` | The cudarc wrapper: context, dedicated non-blocking stream, `DevBuf`, `KernelSet`, CUDA graph capture |
| `src/error.rs` | The error type |
| `src/types.rs` | `Vec3` / `Tensor`, `#[repr(C)]` mirrors of the device structs, with a layout test |
| `build.rs` | The MSVC/nvcc build glue: `vcvars64.bat` capture, CUBIN emission, `/Zc:preprocessor` |
| `src/mesh.rs`, `src/mesh/topology.rs` | The mesh types, the LDU→CSR inversion, and the merged GLOBAL-face-ordered row map (§70) |
| `src/ldu.rs` | The matrix type and the LDU→CSR permutation for AMGX/cuDSS |
| `src/field.rs` | The device field types and `BcKind` |
| `src/pressure/mod.rs` | The `PressureBackend` trait and the measuring selector — hard filter, then accuracy check, then measured timing |
| `src/pressure/fft.rs`, `cuda/pressure.cu` | The cuFFT direct Poisson solver. Method: Swarztrauber, *SIAM Review* 19 (1977) 490; Press et al., *Numerical Recipes* §19.4 |
| `src/pressure/cartesian.rs` | Detecting whether a mesh is a uniform Cartesian block, which is what makes the FFT path applicable — and, §74.8, what EXCLUDES a mesh with 2:1 refinement interfaces from the FFT path, measured by running the detector on one and recording the sentence it prints rather than by asserting it |
| `src/pressure/amgx.rs` | The AMGX backend. AMGX itself is BSD-3-Clause; see `../NOTICE` |
| `src/blockgen.rs` | The structured mesh generator |
| `src/mesh/refined.rs` | The STATIC 2:1-refined hexahedral mesh generator (§74.2): a box born with coarse-fine interfaces, so the operators of §3 can be measured on one. **ORIGINAL** — it emits a plain polyMesh and the geometry sweep of §2 does the rest; nothing here adapts a mesh. Written from **Berger & Colella, *J. Comput. Phys.* 82(1) (1989) 64** (DOI 10.1016/0021-9991(89)90035-1) for what a 2:1 interface IS, and **cited to be distinguished from** — §74.1 argues a face-based cell-centred code needs no flux register and says why; **Isaac, Burstedde & Ghattas, *IPDPS 2012* 426** (DOI 10.1109/IPDPS.2012.47) for the 2:1 balance condition, whose O(1)-ripple algorithm is **named and NOT implemented** (the fixed-point sweep is used instead, and the reason is stated); **Muzaferija & Gosman, *J. Comput. Phys.* 138(2) (1997) 766** (DOI 10.1006/jcph.1997.5853) for locally refined FV on arbitrary polyhedra. All four DOIs in §74 checked against Crossref on 2026-09-01 by fetching `api.crossref.org/works/<doi>`. **p4est is GPL-2.0-or-later, libsc LGPL-2.1 and t8code GPL-2.0 — the licences were checked BEFORE anything was opened and none of the three was opened**, contrary to the brief's assumption that p4est was BSD-2; the papers are read, the code is not. OpenFOAM's `dynamicRefineFvMesh` (GPL-3.0) was not opened either |
| `src/adapt.rs`, `cuda/adapt.cu` | The adapt (§75): the linear-octree `Forest`, the emitter, the refinement criteria, the marking with hysteresis, the 2:1 balance fixed point and the plan. The error indicator is **Löhner, *Comput. Methods Appl. Mech. Engrg.* 61(3) (1987) 323** (DOI 10.1016/0045-7825(87)90098-3), whose ratio is his and whose surface-integral finite-volume form is **DESIGN** — Löhner wrote it for nodal shape functions. The fire resolution measure `D*/dx` is the **FDS User's Guide, NIST SP 1019, "Mesh Resolution"** — a **US Government work, public domain**, present locally at `reference/fds/Manuals/`. The 2:1 balance condition is **Isaac, Burstedde & Ghattas, *IPDPS 2012* 426** (DOI 10.1109/IPDPS.2012.47), whose O(1)-ripple algorithm is **named and NOT implemented**; the leaf-set-only representation and the sort-then-search shape are **Sundar, Sampath & Biros, *SIAM J. Sci. Comput.* 30(5) (2008) 2675** (DOI 10.1137/070681727). **Jasak & Gosman, *Numer. Heat Transfer B* 38(3) (2000) 237 and 257** (DOI 10.1080/10407790050192753, 10.1080/10407790050192762) are the principled estimator and its driver, **named and NOT implemented** and refused by name in §75.9. **ORIGINAL**: the canonical leaf ordering that makes the emitter bit-for-bit identical to §74.2's static generator, the no-free-list rebuild-by-re-sort, and the promotion/cancellation fixed point. Every DOI checked against Crossref on 2026-09-01 by fetching `api.crossref.org/works/<doi>`. **p4est (GPL-2.0-or-later), libsc (LGPL-2.1), t8code (GPL-2.0) and OpenFOAM's `dynamicRefineFvMesh` (GPL-3.0) were not opened** |
| `src/adapt/rebuild.rs` | The addressing rebuild (§75.5): the LDU face order and the cell → face CSR, from one stable sort and two binary searches. **ORIGINAL**, and it is a **correction to the design note this section was written against**: the note specified "two binary searches plus an exclusive scan", and there is no scan — a `lower_bound` over a sorted array already IS the exclusive prefix sum of the per-cell counts, so `cf_offset[c] = lower_bound(owner,c) + lower_bound(nbrKey,c)` exactly, and it is asserted cell by cell. The note also described the within-cell order as "owned faces then neighboured faces", which is **not** what `mesh::topology::build_cell_face_maps` produces; the interleaved ascending order is reproduced by a two-pointer merge, one thread per cell, no atomic. The radix sort is **Knuth, *TAOCP* vol. 3, 2nd ed. (1998) §5.2.5** — a book — and is written to compute what `cub::DeviceRadixSort` computes so that a fully device-resident adapt can substitute the library primitive. No external code of any licence was read |
| `src/adapt/transfer.rs` | The conservative transfer (§75.6): restriction, prolongation and the identity as one pair of gathers. The reconstruction limiter is **Barth & Jespersen, AIAA 89-0366, *27th Aerospace Sciences Meeting* (1989)** (DOI 10.2514/6.1989-366). **Berger & Colella, *J. Comput. Phys.* 82(1) (1989) 64** (DOI 10.1016/0021-9991(89)90035-1) is the restriction/prolongation pair, and its flux-register apparatus stays **cited to be distinguished from** (§74.1). **Balsara, *J. Comput. Phys.* 174(2) (2001) 614** (DOI 10.1006/jcph.2001.6917) is the divergence-free flux prolongation, **named and NOT implemented** — the face flux is not transferred at all and §75.11 says so. **ORIGINAL**, and a **correction to the design note**: the note's multiplicative conservative rescale `lambda = rho phi V / sum rho phihat V` is **singular for any field with zero volume-weighted mean over a parent**, and recentring the reconstruction on the weight-weighted centroid of the children makes the conserved sum telescope exactly with no rescale at all. Measured both ways. DOIs checked against Crossref on 2026-09-01 |
| `src/decompose.rs` | The domain decomposition (§71): the partition map, the ghost layer, the local renumbering and the exchange plan. The Hilbert index is written from **Skilling, *AIP Conf. Proc.* 707 (2004) 381** (DOI 10.1063/1.1751381) and **Butz, *IEEE Trans. Computers* C-20(4) (1971) 424** (DOI 10.1109/T-C.1971.223258) — papers, which `LICENSING.md` permits; the SFC-as-partitioner argument is **Aluru & Sevilgen, *HiPC* (1997) 230** (DOI 10.1109/HIPC.1997.634498) and the communication-volume metric is **Catalyurek & Aykanat, *IEEE TPDS* 10(7) (1999) 673** (DOI 10.1109/71.780863). **METIS 5.2.x is Apache-2.0** (`LICENSE` at `github.com/KarypisLab/METIS`, verified 2026-08-31) and **ParMETIS is proprietary/non-commercial** (`LICENSE` at `github.com/KarypisLab/ParMETIS`, verified 2026-08-31); neither is linked and neither source was read — §71.2 gives the reasons. The zero-halo-layer style, in which a cut face becomes an ordinary coupled boundary face, is described in **Weller, Tabor, Jasak & Fureby, *Computers in Physics* 12(6) (1998) 620** (DOI 10.1063/1.168744), a paper; OpenFOAM's source was not opened |
| `src/exactsum.rs`, `src/exactsum/tests.rs`, `cuda/exactsum.cu` | The order-free accumulator (§72): reductions whose answer is a function of the multiset of terms, so that a cut cannot move one. **Derived** from the binned / indexed floating-point literature - **Demmel & Nguyen, *ARITH-21* (2013) 163** (DOI 10.1109/ARITH.2013.9) and ***IEEE Trans. Computers* 64(7) (2015) 2060** (DOI 10.1109/TC.2014.2345391); **Ahrens, Demmel & Nguyen, *ACM TOMS* 46(3) (2020) 1** (DOI 10.1145/3389360); **Collange, Defour, Graillat & Iakymchuk, *Parallel Computing* 49 (2015) 83** (DOI 10.1016/j.parco.2015.09.001); **Kulisch, *Computer Arithmetic and Validity*, 2nd ed., De Gruyter (2013)** (DOI 10.1515/9783110301793) - all **papers and a book**, every DOI checked against Crossref on 2026-08-31. **ExBLAS and ReproBLAS are BSD-3-Clause and would have been readable; neither was opened**, and the limb width `W = 30`, the limb count `K = 4`, the single GLOBAL anchor and the gather-not-reduce collective are ORIGINAL choices argued in §72.2-§72.4. **Iakymchuk et al., *J. Comput. Appl. Math.* 371 (2020) 112697** (DOI 10.1016/j.cam.2019.112697) is the closest published work and is cited as such. **Balaji & Kimpe, *HPCC/EUC* (2013) 407** (DOI 10.1109/HPCC.and.EUC.2013.65) is why no `MPI_Allreduce` or `ncclAllReduce` appears anywhere in the design; NVIDIA's NCCL environment-variable documentation, which promises no reduction order, is the other half of that argument. No GPL source was opened |
| `src/distsolve.rs`, `src/distsolve/tests.rs` | PCG and PBiCGStab over a decomposed mesh (§73). **ORIGINAL**: every kernel launched is one `cuda/solver.cu` or `cuda/precon.cu` already owns, unchanged, so the arithmetic is the serial solver's; what is new is where the exchange goes and which reduction is used. The recurrences are **Hestenes & Stiefel, *J. Res. NBS* 49(6) (1952) 409** (DOI 10.6028/jres.049.044, a **US Government work, public domain**) and **van der Vorst, *SIAM J. Sci. Stat. Comput.* 13(2) (1992) 631** (DOI 10.1137/0913035), as §8 already cites them; **Saad, *Iterative Methods for Sparse Linear Systems*, 2nd ed., SIAM (2003)** (DOI 10.1137/1.9780898718003) §6.7, §7.4.2, §12.4 and **ch. 14**, which is where block Jacobi is defined and is what the block-local factorisation is named as. **Cai & Sarkis, *SIAM J. Sci. Comput.* 21(2) (1999) 792** (DOI 10.1137/S106482759732678X) is cited **to be distinguished from, not followed**. **Ghysels & Vanroose, *Parallel Computing* 40(7) (2014) 224** (DOI 10.1016/j.parco.2013.06.001) and **Cools & Vanroose, *Parallel Computing* 65 (2017) 1** (DOI 10.1016/j.parco.2017.04.005) are the pipelined variants, **refused by name in §73.7** on the strength of **Cools, *Parallel Computing* 86 (2019) 16** (DOI 10.1016/j.parco.2019.05.002), which documents their accuracy loss. **Amdahl, *AFIPS '67 (Spring)* 483** (DOI 10.1145/1465482.1465560) is the shape of §73.6's scaling statement and of the one it declines to make. Every DOI here checked against Crossref on 2026-09-01 by fetching `api.crossref.org/works/<doi>`. All **papers and a book**; no solver source of any licence was opened, and no GPL source was consulted |
| `src/xref.rs` | The citation audit (§80): the Markdown heading/equation-label parser, the prose lexer that separates comments and string literals from code, the two-line attribution window, and the tests that hold every `§NN.M`, `SNN.M` and `(NN.M)` in the tree to something that exists. **ORIGINAL** and test-only (`#[cfg(test)] mod xref;` in `lib.rs`), so the release build is unchanged by construction. Its only inputs are `SPEC-LIT.md`'s own structure and the tree on disk; no external source of any kind was consulted, and the escape-hatch registry — Patankar, Jasak, Moukalled, Saad, Perić, Incropera, Modest, Numerical Recipes, and the two `docs/` documents — is parsed out of §80.3 rather than written here |
| `src/mesh/gpugeom.rs`, `src/mesh/gpugeom/tests.rs`, `cuda/meshgeom.cu` | The finite-volume geometry sweep on the device (§82). **ORIGINAL** — a line-for-line device restatement of this crate's own `src/mesh/geometry.rs`, whose physics and whose citations are §2's and are unchanged: **Jasak (1996) §3.2, §3.3.1, §3.4.2**, **Moukalled, Mangani & Darwish (2016) §6.4, §8.6.4**, **Ferziger & Perić (2002) §8.6**. Nothing is re-derived here; the section's own claim is about arithmetic reproducibility, for which the sources are **Goldberg, *ACM Computing Surveys* 23(1) (1991) 5** (DOI 10.1145/103162.103163) for the non-associativity of floating-point addition that makes the gather order a specification, and the ***NVIDIA CUDA C++ Programming Guide*** plus the `nvcc` reference for `-fmad`, read from NVIDIA's own documentation. **ORIGINAL to §82**: the reformulation of the host sweep's two per-cell SCATTERS as one gather over the cell → face CSR in the order that makes them bitwise; the per-unit `-fmad=false` and the second, CONTRACTED cubin that measures what the flag buys rather than asserting it; and the measurement of §82.2, which finds that §75.8 named the wrong half of the rebuild as its binding constraint. **OpenFOAM's `primitiveMesh`/`surfaceInterpolation` (GPL-3.0) and SU2 (LGPL) are the obvious references for a finite-volume geometry sweep and neither was opened** |
| `src/capture.rs`, `src/capture/registry.rs`, `src/capture/gates.rs` | The CUDA-graph capture gate (§81): the capture/replay/compare protocol, the node-census check, the source-derived registry that classifies every module which reaches the device, and the thirty-six gates themselves. **ORIGINAL** and test-only (`#[cfg(test)] mod capture;` in `lib.rs`), so the release build is unchanged by construction. Its inputs are this crate's own modules and the tree on disk; the CUDA Graph semantics it relies on — `cuStreamBeginCapture`, `cuGraphGetNodes`, `cuGraphNodeGetType`, and the fact that `cuMemAllocAsync` is stream-ordered and therefore capturable — are the CUDA Driver API's published interface, read from NVIDIA's documentation. No external source code of any kind was consulted |
| `src/halo.rs`, `cuda/halo.cu` | The ghost-cell exchange (§71.4): the pack-as-gather kernel and the contiguous-receive layout that removes the unpack entirely. Original; no external source |
| `src/bin/decompose.rs` | `ofgpu-decompose`, the driver that cuts a shipped case into parts, runs it in one process and reports whether any bit moved (§71.9), then reduces over every relabelling of that cut and reports whether the reduction moved (§72.9), then runs a real PCG and PBiCGStab over the cut and over every relabelling of it and reports whether the field or the stopping iteration moved, with the block-local DIC/DILU iteration ladder and the per-iteration cost (§73.9) |
| `src/bin/*` | Drivers and benchmarks |
| `src/bin/vof.rs` | The two-phase driver and its surge-front report |
| `src/bin/common/mod.rs` | The drivers' shared case-reading seam, `CaseNumerics` included — see the *DESIGN* table above for the §13.4.1 row |
| `src/bin/probe.rs`, `cuda/probe.cu` | The toolchain probe: nvcc → CUBIN → cudarc → launch → read-back, reproducing the CPU answer bit for bit |
| `src/device.rs` (§81.3, §81.4) | The capture guard and the graph node census, added on top of the existing cudarc wrapper: an `AtomicBool` that makes every host round-trip on `Gpu` refuse itself **by name** while a capture is recording, and `Graph::shape()`, which walks the instantiated graph with `cuGraphGetNodes`/`cuGraphNodeGetType` so that an allocation node inside an iteration is visible — stream-ordered allocation is *capturable*, so that defect otherwise succeeds silently. Original; the two driver entry points are documented API |
| `cuda/ofgpu_device.cuh` | The only header the kernels share: `ofscalar`, `ofvec3`, `oftensor` and the device-side helpers. Its layouts are mirrored by `src/types.rs`, and that pairing is pinned by a layout test |
| `src/lib.rs` | The crate root: module declarations, the crate-wide `Scalar` alias, the re-exports. No numerics of its own |
| `../tools/*.py` | The isometric volume renderer and the HTML report builder |
| `../tools/radcal_emissivity/` | The standalone RADCAL driver behind §62.12's Gate 1-E. `stubs.f90` is four symbols' worth of stand-in for three FDS modules and `emissivity_driver.f90` is one call to NIST's own `SUB_RADCAL` per grid point; both are original, and the model they drive is NIST's public-domain `rcal.f90`, which is neither copied nor modified. Nothing in the build depends on it |

## Case format interoperability — format

Written from the format as it appears in data files, so that existing pre- and
post-processing tools (ParaView, `foamToVTK`) work with ofgpu output. ofgpu
links against no part of OpenFOAM and contains no OpenFOAM source.

| File | What it reads or writes |
|---|---|
| `src/io/tokenizer.rs` | The ASCII tokeniser |
| `src/io/dict.rs` | Dictionaries, `#include`, `$var` lookup, `Switch`, dimensioned scalars |
| `src/io/polymesh.rs` | `constant/polyMesh`: points, faces, owner, neighbour, boundary |
| `src/io/fields.rs` | `volScalarField` / `volVectorField` files and their patch entries |
| `src/io/case.rs` | `system/fvSolution`, `system/fvSchemes`, `system/controlDict`, `constant/*` |
| `src/io/regex.rs` | The pattern matcher for `boundaryField` and `solvers` keys. Written from scratch — a backtracking matcher over the subset the format uses, anchored at both ends. No regex crate is a dependency |
| `src/io/contract.rs` | The unsupported-setting contract of SPEC-LIT §13.4 |
| `src/io/regex.rs` | The POSIX ERE matcher a quoted dictionary key needs. Written from IEEE Std 1003.1 §9.4, Kleene (1956) and Thompson (1968) |
| `src/io/contract.rs` | SPEC-LIT §13.4: a setting this solver cannot honour fails loudly, and `-permissive` is the one escape hatch. Ours by design |

The `FoamFile` banner these write is ofgpu's own, and states plainly that the
file is written by ofgpu in that format. It is not the upstream banner.

## Permissive references

| Source | Licence | Used for |
|---|---|---|
| `../reference/fds` | US Government work (NIST), public domain | The low-Mach formulation of Rehm & Baum (1978), and fire-specific models. Any file that adapts from it says so and acknowledges NIST |
| AMGX | BSD-3-Clause | Linked as the algebraic-multigrid backend |
| cudarc | MIT OR Apache-2.0 | Cargo dependency |
| AcademySoftwareFoundation/openvdb, `v13.0.0` | Apache-2.0 | The `.vdb` archive format has no published byte-layout document, so the implementation is the only authority for it. Read file by file - `io/{Archive.cc,GridDescriptor.cc,Compression.h}`, `tree/{Root,Internal,Leaf}Node.h`, `math/{Transform.cc,Maps.h,Mat.h}`, `Grid.cc`, `Metadata.h`, `util/{Name.h,NodeMasks.h}` - to write an INDEPENDENT writer (`src/io/vdb.rs`), first at `fp32` and, for SPEC-LIT §45, for the `_HalfFloat` save preference. Every citation is inline at the byte it explains. No source copied; nothing linked. The NanoVDB layout in `src/io/nvdb.rs` comes from the same project's Apache-2.0 PNanoVDB/NanoVDB headers |

## Validation

`SPEC-LIT.md` §10 and §22 are the list, and the rule is absolute: **no test
compares against another CFD code.** Method of manufactured solutions,
analytical solutions, and published benchmark data — Ghia, Ghia & Shin (1982);
Moser, Kim & Mansour (1999); Driver & Seegmiller (1985); McCaffrey (1979);
Martin & Moyce (1952).

One note on the last of those. `ofgpu-vof -surge` and the `#[ignore]`d
`a_dam_break_surge_front_stays_under_the_ritter_bound` in `src/vof.rs` print
the surge front in Martin & Moyce's dimensionless variables so that a reader
holding the paper can lay one on the other. **Their table is deliberately not
transcribed into this repository**, because nobody working on it has the paper
to transcribe from, and a table of numbers attributed to a 1952 experiment and
actually recalled would be worse than no table. What those tests *assert* is
analytic: exact boundedness of `alpha`, exact conservation of the phase volume,
and a surge front that never outruns the Ritter (1892) shallow-water
characteristic speed `2 sqrt(g h0)`.

Agreement with another solver to 1e-13 is not evidence that this code is
correct. It is evidence of something else entirely, and it is exactly what this
project spent a phase removing.
