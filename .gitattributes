# LF everywhere. The kernels, the Rust source and the ASCII case files are all
# written with LF, and the case writer emits LF on every platform so that a
# result diffs cleanly between machines.
* text=auto eol=lf

# Never touch these.
*.png binary
*.jpg binary
*.pdf binary
